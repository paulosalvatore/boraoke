# boraoke — Manager Log

## 2026-08-19 (later) — heartbeat fire (Product TM, unattended) — 🟢 PR #62 / TICKET-86 MERGED + auto-deployed: the last spoofable-XFF copy is gone

**Cold-resumed from disk, not from memory.** Step 0 behind-origin guard: canonical clone `~/Documents/GitHub/boraoke` (`git@github.com:paulosalvatore/boraoke.git`), HEAD on `main`, **0 behind `origin/main`** — board authoritative, not the abandoned `cantai` duplicate. Step 0.5 re-verified against reality rather than the board: **boraoke.com HTTP/2 200**, `origin/main` at `81849e7`, open PRs = #58 (TICKET-85 spike) and #60 (TICKET-84 design directions).

**Concurrency observed first, and respected — the same concurrent driver as the earlier fire is still live.** Four sibling worktrees were in flight: `.worktrees/ticket-82` (TV player remount — committed 5 minutes before selection, dirty tree, **a live Playwright worker plus a `next dev` on port 3190**), `ticket-83` (search mode-first pagination — 42 min, dirty), `ticket-84` (design directions — 27 min, **a live `next dev` on port 3192**, open as PR #60), `ticket-85` (YouTube-independence spike — 20h, clean, open as PR #58). All four treated as **CLAIMED**: no file, branch, worktree or PR of theirs was touched, and neither open PR was merged on their behalf. Both of #58 and #60 remain open and are the other driver's to deliver.

**Increment picked: TICKET-86 — filed and delivered this fire.** It is the follow-up the earlier 2026-08-19 fire surfaced and deliberately left unfiled: `app/api/t/route.ts` carried a **second, divergent copy** of the pre-TICKET-78 IP derivation, trusting the client-suppliable first hop of `x-forwarded-for` ahead of the edge-set `x-real-ip`. That value is the IP half of the beacon's dual-bucket limiter (`beaconRateLimitOk`, 300 events/min/IP), so a caller rotating a forged XFF spread its beacon traffic across unlimited IP buckets, leaving only the session bucket — which is itself client-minted (`uuid`/`sessionKey`), exactly the rotation the dual-bucket design exists to defend against. MED, not HIGH: the endpoint is fail-open, write-only telemetry that authenticates nothing and mutates no product state, so the exposure is telemetry-store pollution, not queue or account compromise.

**Chosen over the alternatives for concurrency safety, not just priority.** TICKET-79 touches root `app/layout.tsx` (all-routes blast radius, plausible collision with 82/83); TICKET-80 needs a TL timing call; TICKET-81 needs a real notched iPhone. TICKET-86 touches `app/api/t/route.ts` + `__tests__/api-t.test.ts` — verified file-disjoint from all four in-flight branches (83 touches `app/api/search/route.ts`, not `/api/t`; none touches the telemetry files).

**The fix, and the one seam that mattered.** The route now **imports** the already-hardened `clientIpFrom` from `lib/host-auth.ts` rather than keeping a divergent copy — `lib/host-auth.ts` itself is byte-unmodified (reviewer confirmed the blob hash `d23d0d5` is identical on both sides), deliberately, because four sibling branches are concurrently in flight against that file's history. The seam: `clientIpFrom` returns the sentinel `"unknown"` when no header is present, while `beaconRateLimitOk` documents `""` as "this bucket does not apply". Mapping `"unknown"` → `""` in the route preserves today's fail-open, IP-bucket-not-applied behaviour for header-less requests (unit tests, local dev) **exactly**, while closing the spoof for real requests. Pinned by its own test so the choice cannot be silently reversed.

**Gate chain — GREEN.** Dev: full `npx jest` **764 pass / 44 suites**, `npx next build` exit 0, `tsc --noEmit` clean of new errors (only the known `__tests__/youtube.test.ts` + `e2e/advance-auth.spec.ts` baseline). App Tester **N/A** (backend-only, no UI). Cyber **folded into the D-022 opus Reviewer** (security-hardening change) → **PASS**. **Reviewer opus D-022 → APPROVE**: re-derived every claim from source rather than the Dev report — diff is exactly 4 paths, `lib/host-auth.ts` blob-identical, trust ordering verified by reading both files, sentinel mapping verified against the limiter's actual implementation (`const ipOk = ip ? bucketOk(...) : true`), the other four `clientIpFrom` callers proven un-regressable, and gates reproduced independently (764/44, build 0). **Playwright e2e deliberately NOT run by anyone** — sibling worktrees had live Playwright + dev servers, and the 2026-08-08 session already recorded that concurrent suites produce dozens of false failures; this change has no UI surface.

**The new test is discriminating, verified twice.** Both the Dev and the Reviewer independently reverted the route to the old XFF-first body and reran `npx jest __tests__/api-t.test.ts`: **12 pass / 1 fail**, the failure being exactly the new spoof test (`Expected: 204, Received: 202` at `api-t.test.ts:169`), then restored and got 13/13. The sentinel test correctly passes under both implementations — by design, it pins unchanged behaviour.

**Merged + deployed.** Advisory GitHub Actions `build-and-test` polled to COMPLETED/SUCCESS, `mergeable=MERGEABLE`; squash-merged PR #62 → **`f9c905b`**. In-tier per D-043 + the 2026-08-06 merge-cadence decision (TM auto-merges gate-green backend/test/docs-only PRs) and deploy-autonomy-non-live: boraoke is pre-launch/non-revenue (no payments, no real venue accounts), and no money/PII/DNS/irreversible boundary was crossed. Verified after the fact: `clientIpFrom` import present in `origin/main:app/api/t/route.ts`, a **Production deployment for `f9c905b` created at 12:25:47Z**, `curl -sI https://boraoke.com/` → HTTP/2 200. As with TICKET-78 this is a backend change with no visible content signal, so the assertion is code-on-main + production-deployment-created + site-healthy, not a content diff.

**Two LOW follow-ups from the reviewer, filed here rather than as tickets (neither blocking, both cosmetic-to-theoretical):**
- **Sentinel-string collision.** A request whose derived IP is the *literal* string `"unknown"` now maps to `""` and skips the IP bucket, where the old code would have charged an `ip:unknown` bucket. Not exploitable on Vercel (the edge overwrites `x-real-ip`), and anyone who controls that header can already rotate values freely — a theoretical narrowing, not a new hole. The durable fix is to make "no header at all" structural (`clientIpFrom` returning `null`) rather than a magic string, which would also delete the translation seam.
- **Stale test title.** `__tests__/api-t.test.ts:133` still says "(first x-forwarded-for hop)"; XFF is now the documented fallback, not the primary path. Cosmetic.

**Process note from the reviewer, recorded not buried:** no `work/reports/dev/TICKET-86-*.md` was committed on the branch — the ticket file carried the record, and the reviewer re-derived every claim from source instead of from a report, so the verdict did not rest on it. `reviewer.md` step 3 normally expects a dev report on the branch; flagged as a gap in this fire's paperwork, not in its evidence.

**Housekeeping.** Own worktree `.worktrees/ticket-86` removed (clean tree, nothing unpushed, PR MERGED-verified first), local + remote branch deleted, local `main` fast-forwarded to `f9c905b`. The four concurrent-driver worktrees left exactly as found. This entry, the BOARD.md banner and the branch event-log file are the only status writes.

**Outcome:** `progressed` — one bounded security-hardening PR filed, built, opus-reviewed, merged and deployed end-to-end, closing the last divergent copy of the TICKET-78 pattern, without touching any of four concurrently-claimed worktrees or either of the other driver's open PRs.

## 2026-08-19 — MANUAL heartbeat fire (Product TM, dispatched by Global TM while the launchd fleet was API-degraded) — 🟢 PR #59 / TICKET-78 MERGED + auto-deployed

**A manual gap-fill heartbeat, not the scheduled launchd fire.** Cold-resumed from this board + MANAGER-LOG; re-derived state against `git`/`gh` at `main` `f9f648d`.

**Concurrency observed first, and respected.** Four sibling worktrees were in-flight from a concurrent driver — `.worktrees/ticket-82` (TV player remount), `ticket-83` (search mode-first pagination), `ticket-84` (design directions), `ticket-85` (YouTube-independence spike, open as PR #58). A **live Playwright worker process** was running inside `.worktrees/ticket-82` (pid 13106) plus active playwright-mcp processes — so all four were treated as CLAIMED and left untouched (no files, no branches, no PRs). None of them was raced. Selection deliberately picked a backlog item **file-disjoint** from all four.

**Increment picked: TICKET-78** (spoofable rate-limit IP bucket) — MED/S, backend-only, single isolated file (`lib/host-auth.ts`), unit-testable, no UI, no route-shape change, and verified disjoint from the four in-flight branches (`git diff --name-only origin/main <branch> -- lib/host-auth.ts …` empty for each). Chosen over TICKET-79 (touches root `app/layout.tsx` — all-routes blast radius, possible collision with 82/83), TICKET-80 (needs a TL timing call), TICKET-81 (needs a real notched device).

**The fix.** `clientIpFrom` derived the per-IP bucket key from the **first hop of client-suppliable `x-forwarded-for`** — an attacker could rotate the claimed IP per request to spread a failed-login/submit flood across many buckets. Now trusts the **edge-set `x-real-ip` first** (Vercel writes it at the edge and overwrites client-supplied values, so it is not client-controllable), with an optional `TRUSTED_CLIENT_IP_HEADER` env override for non-Vercel proxies (e.g. `cf-connecting-ip`) taking precedence, and XFF first-hop as a dev/non-Vercel fallback, ending at the unchanged shared `"unknown"` bucket. No change to the per-`patronUuid` backstop. On the normal Vercel path `x-real-ip` == XFF-first-hop, so production bucketing is unchanged in the common case; the derivation only differs when the two differ (a forged XFF). Added a 5-test `clientIpFrom` block to `__tests__/host-auth.test.ts`.

**Gate chain — GREEN.** Dev: `npx jest` **762 pass / 44 suites**, `npx next build` exit 0, `tsc --noEmit` shows only the pre-existing baseline errors (youtube.test.ts / e2e specs — none in the changed files). App Tester **N/A** (backend-only, no UI). Cyber **folded into the D-022 opus Reviewer** (security-hardening change). **Reviewer opus D-022 → APPROVE-WITH-FOLLOWUPS**: independently confirmed the diff is exactly the two files (+ the append-only event-log line), the trust ordering closes the spoof vector and degrades safely, all three callers use the return as an opaque bucket key (no regression), and the new tests are **discriminating** (test 1 returns the forged `1.1.1.1` against the old impl). The reviewer's sole follow-up (restore `TRUSTED_CLIENT_IP_HEADER` in the test to avoid cross-file env leakage) is **already satisfied** by the pre-existing file-level `afterEach` at `host-auth.test.ts:36` that resets `process.env` after every test — verified, so no change was needed.

**Merged + deployed.** GitHub advisory `build-and-test` polled to **COMPLETED/SUCCESS**, `mergeable=MERGEABLE`; squash-merged PR #59 → `f9f648d`. boraoke is non-live/pre-revenue (no payments, no accounts) so this is in-tier auto-merge+deploy per D-043 + deploy-autonomy-non-live — no money/PII/DNS/irreversible boundary crossed. Vercel auto-deploys on push to `main`; `curl -sI https://boraoke.com/` → HTTP/2 200; fix confirmed present in `origin/main:lib/host-auth.ts`. (This is a backend change with no visible content signal to content-verify, unlike the 2026-08-08 CSS deploys — the assertion here is code-on-main + site-healthy.)

**Follow-up surfaced (not filed as a ticket this fire — flagged to the Global TM):** `app/api/t/route.ts` carries a **duplicate** of the same XFF-first-hop pattern (telemetry `ipFrom`, lower sensitivity), left untouched to keep this increment bounded. Worth a small follow-up ticket.

**Housekeeping.** Own worktree `.worktrees/ticket-78` removed (clean tree, nothing unpushed, PR MERGED-verified), local + remote branch deleted, local `main` fast-forwarded to `origin/main` (`f9f648d`); the four concurrent-driver worktrees left intact. This entry + BOARD.md + the branch event-log file are the only status writes.

**Outcome:** `progressed` — one bounded security-hardening PR shipped end-to-end (dev → opus review → merge → deploy → live-verify) without touching any of the four concurrently-claimed worktrees.

## 2026-08-08 — TM interactive session, TL PRESENT (D-043 merge authority), continuation — 🟢 FIVE MORE PRs MERGED + CONTENT-VERIFIED LIVE; 1 TL request rejected on security grounds; 1 near-miss worktree-removal corrected; 4 new tickets filed

**Continuation of the same 2026-08-08 interactive session recorded below.** Written for a stranger; re-derived against `git log` / `gh pr list --state all` on `main` at `0f58c7b`, not carried forward from memory.

**A note on the brief this entry is based on, for accuracy's sake:** the handoff described "six PRs merged this round." `git log` and `gh pr list --state all --json number,headRefName,mergedAt,title` both show only **five** distinct PR numbers merged this round — #53, #54, #55, #56, #57 — because TICKET-73 was folded into PR #55's diff (title: "…incl. TICKET-73 viewportFit") rather than merged as its own PR. Recorded as five below; the ticket itself still gets its own row/status on the board.

**Merged this round (5 PRs, all squash-merged to `main`, all now LIVE and content-verified — title text, sitemap content-type, `viewport-fit`, and the softened free-promise copy all confirmed on boraoke.com, not merely an HTTP 200):**
- **PR #53 / TICKET-76** (`11b8b33`) — rolling 30-day host session, was 12h, so saved rooms open straight into admin. **The Tech Lead's literal request — prefill the host code — was rejected on security grounds, recorded as such rather than silently substituted:** only `hostCodeHash` is stored server-side, so the raw code is a non-rotatable, unrecoverable credential; a URL leaks it via browser history, `Referer` headers, screenshots, and server logs; `localStorage` would turn any XSS on the page into a permanent room takeover. Cookie attributes are byte-identical to before apart from the lifetime.
- **PR #54 / TICKET-75** (`e278106`) — room language now seeds from the creator's `NEXT_LOCALE` at creation, plus the TV `<html lang>` mismatch fixed. **Root cause:** `createRoom` never wrote `settings.language` on any code path, so the TV's own (correct) follow-the-room-language policy was obeying a field that was permanently unset. **Ripple worth recording:** a patron with no locale cookie who joins a *new* room made by an English-speaking host will now see English — existing rooms and any patron with an explicit cookie are unaffected.
- **PR #55 / TICKET-74 (+TICKET-73)** (`98721ab`) — metadata/SEO realigned from the original bar-only framing to the shipped venue-agnostic positioning, across all three locales; the free-promise footer softened from a permanent-sounding claim to present tense; `viewportFit: "cover"` added to `app/metadata.ts` (TICKET-73). **Finding worth recording on its own: `/sitemap.xml` was a soft-200.** It resolved with `content-type: text/html` and 26,209 bytes — the room-not-found page, silently swallowed by the dynamic `[room]` catch-all route — not a sitemap. No real sitemap had ever existed, despite `robots.txt` advertising one since TICKET-33. It now serves real `application/xml`.
- **PR #56 / TICKET-77** (`d8f5fc1`) — an analytics link in the admin header (shown only on an authorised session) plus a confirm-gated "Sair" logout control. **Before this PR, `POST /api/host/session` — the logout endpoint — had zero callers anywhere in the app.** No logout UI existed at all; a host had no way to end their own session short of clearing cookies by hand.
- **PR #57 / TICKET-72** (`0f58c7b`) — feedback discoverable on mobile without reintroducing the TICKET-71 row-overlap: a compact 40×40 icon-only trigger, portalled into the page's own `<header>`, `position: static` (not `fixed` — a normal-flow element in the header can't collide with a scrolled queue row, the same geometric-guarantee approach TICKET-71 used).

**Merge sequencing worth recording as a deliberate call, not an accident.** PR #53 (the 30-day session) was held back until PR #56 (the logout control) merged first — shipping a 30-day rolling session with no way to end it would have extended host access roughly 60× over the prior 12h with zero UI to revoke it. Once both were in, the composition was verified on the wire before merging #53 at all: logging out clears a 30-day session, and a **post-logout visit to the public landing page does not re-arm it** — the rolling-refresh logic re-sends the cookie taken from the *current request*, and only after `requireHost()` verifies that cookie is still valid, so a caller without a valid session can never mint a fresh one just by hitting a page.

**A near-miss during this round, recorded plainly rather than glossed over.** The TICKET-74 worktree was removed by the Tech Manager while `worktree-cleanup`'s own report still showed 2 unpushed commits — because the cleanliness check was chained with `&&` ahead of the removal command rather than gating it, so a non-zero/incomplete check result didn't actually stop the `rm`. **No work was lost**, verified two ways: the diff between the local branch tip and the merged `main` (excluding the append-only event log) was empty, and the PR's squash commit `203e1f7` carried the branch's full content. The guard has since been rewritten so the check result actually gates the removal, not just logs a warning next to it.

**Sibling-worktree contention is real at this concurrency, recorded so it isn't re-discovered the hard way.** Three separate agents this round reported contaminated e2e runs — spurious failures traced to concurrent Playwright suites running in sibling worktrees stepping on shared resources (one run saw 67 false failures, another saw 31), and all three were correctly diagnosed as environmental rather than product bugs before any code was "fixed" to chase a phantom. Recording the concrete number for next time: **cap test-heavy tickets at roughly 3 concurrent agents.**

**Agents stalling on backgrounded work — a recurring pattern this round, filed upstream rather than patched locally.** Six separate occurrences across five different agents ended their own turn while still waiting on a backgrounded test suite or a child agent, rather than staying live until the result landed. This is a framework-level behavior, not a boraoke-specific one, so it was filed to the framework's self-improvement inbox as `work/self-improvement/inbox/2026-08-08-subagents-stall-on-backgrounded-work.md` **in the framework repo** (`agentic-software-house`), not here — per this repo's own rule that framework fixes route through that repo's own intake, never inline. **TICKET-72 additionally completed all of its work and never opened its PR at all** — it sat invisible on a branch until someone went looking, the same silent-parking failure class as the stall pattern above, just one step further along (work done, delivery never surfaced).

**Four new tickets filed this round, all as bounded, actionable items — none held as a blanket "later":**
- **TICKET-78** — `clientIpFrom` (`lib/host-auth.ts:234`) trusts a client-supplied `x-forwarded-for` with no trusted-proxy pinning, so the IP-side rate-limit bucket is spoofable in principle. Pre-existing, found incidentally by the TICKET-72 reviewer while auditing an unrelated diff. The per-`patronUuid` bucket is the backstop, so this is MED not HIGH.
- **TICKET-79** — TICKET-75's `<html lang>` fix on the TV route was implemented as an inline client-side script because `app/layout.tsx` was off-limits to that ticket; with JS disabled the *served* HTML still carries the wrong value, and the same mismatch is unfixed on `app/(patron)/[room]/page.tsx`, which TICKET-75 never touched. Needs the fix done properly in the root layout. MED, size S.
- **TICKET-80** — per-venue analytics. `/admin/analytics` is global and gated on the site-wide `HOST_TOKEN`, so a venue host cannot reach analytics for their own room at all — the per-room host dashboard shows only live in-session queue counters, no history. This is the thing a real venue host actually wants; needs a room-scoped authenticated endpoint. Recorded explicitly as needing a Tech-Lead call on whether it lands near-term or waits for the Phase-2 accounts work, since a real per-venue accounts model may be the more natural place to hang this auth than a one-off room-scoped path built ahead of it.
- **TICKET-81** — notch/safe-area layout follow-ups exposed by turning on `viewportFit: cover`: the `/[room]` language pill sits roughly 15px inside a 47px notch band, and the landing `<header>` overflows 13–16px at 320px width. Also records, so it isn't lost: **TICKET-73's real-device acceptance criterion was never actually met** — Chromium's CDP `Emulation.setSafeAreaInsetsOverride` forces the same inset values whether or not `viewport-fit=cover` is present (measured both pre- and post-TICKET-74), so it structurally cannot distinguish "wired but inert" from "wired and active." A real notched iPhone is still required to close that out.

**Board updates.** `work/status/BOARD.md`: a new banner prepended for this continuation (old banner kept below per the append-for-dated-history convention); TICKET-72/73/74/75/76/77 flipped to MERGED + LIVE in the Tickets table with PR numbers and squash SHAs; the "not yet decided" TICKET-72/73 line in Needs-user edited in place to record both are now resolved (TICKET-72's discoverability question answered by PR #57; TICKET-73 shipped but its real-device criterion stays open under TICKET-81); four new ticket rows added.

**Selection safety / scope.** Status docs (this entry + `BOARD.md`) and four new ticket files only — no product code, no branch, no worktree, no PR touched by this durable-record update itself. Did **not** touch `agentic-software-house` (the framework repo) beyond the one sanctioned inbox-note commit described above, which happens in that repo's own tab per its own rules, not this one.

**Outcome:** `progressed` — five PRs shipped and content-verified live, one Tech-Lead request correctly declined and recorded rather than silently substituted, one process near-miss disclosed with its fix, a real concurrency limit and a stall pattern both surfaced for the record, and four new findings filed as actionable tickets rather than absorbed into a vague backlog line.

## 2026-08-08 — TM interactive session, TL PRESENT (D-043 merge authority) — 🟢 FOUR PRs MERGED + VERIFIED LIVE; a refuted fix redesigned; 4 TL decisions; 2 tickets filed

**This was an interactive Tech-Manager session with the Tech Lead present in the room — NOT a heartbeat fire.** Written for a stranger: everything below is re-derived against `git log` / `gh pr list --state all` on `main` at `de9e98c`, not carried forward from memory.

**Merged this session (4 PRs, all squash-merged to `main`, all now LIVE):**
- **PR #49 / TICKET-66** (`118ad06`) — split the `--accent` token by role to clear WCAG AA on every CTA. `--accent` (`#e63946`) stays the brand hue for borders/focus/large text; new `--accent-strong` (`#d92330`) fills white-text CTAs (4.17→4.96:1); new `--accent-text` (`#ee5a64`) is the accent used as text on dark surfaces. Fixed the two originally-filed failures plus a **latent third** the TICKET-60 suite never covered (accent-as-text on `--surface`, 4.18:1) and two previously-uncatalogued white-on-accent CTAs (`.confirmYes`, `.pendingBadge`). Deliberately did **not** convert `analytics.module.css .button` — it pairs `--accent` with dark text at 4.75:1, and a mechanical sweep would have dropped it to 3.99:1, a regression. Both `test.fixme` blocks in `e2e/contrast.spec.ts` are now real, passing tests.
- **PR #50 / TICKET-70** (`be59814`) — stop the TV up-next rail truncating singer names to two characters. Root cause was **not** the initial guess: the QR/"powered by" `.join` card had no width cap, so it sized itself to the room's join URL — the bug scaled with slug length and squeezed the up-next name column. Round 1 only moved the truncation threshold from ~2 chars to ~6; the TM rejected that as insufficient and it was reworked — the table badge now shares a row with the song title, doubling the name column (110→231px @1920px). Real Brazilian names up to 11 characters now render in full.
- **PR #52 / TICKET-69** (`cf1336e`) — rebuilt the landing page as Direction 2 "Demo vivo": a static `/tv` mock + QR phone card hero, venue labels, three bullets, a join strip, a free-promise footer. `SavedRooms`, join-by-code, and `LanguageSwitcher` all preserved. 47 `Landing` i18n keys, identical key sets across pt-BR/en/es. Advertises only features that actually ship (no accounts, theming, venue presets, or payments).
- **PR #51 / TICKET-71** (`de9e98c`) — stop the mobile feedback pill covering queue rows and the CANTAR button.

**The TICKET-71 refutation — the most instructive event of the session, recorded in full.** The first delivery kept the pill `position: fixed` on mobile and added a runtime JS collision-avoidance loop (`useCollisionLift`) that nudged the pill clear of whatever it currently overlapped, capped at 50% of viewport height. It passed a 5-row smoke test checking only scroll fractions 0.0 and 1.0, and an opus review that probed the same two extremes. An **independent verifier** swept a 25-song queue across the full scroll range and found the lift **saturating its cap from roughly scroll-fraction 0.2 to 0.9 while still overlapping 1–2 rows**. The decisive experiment: stubbing the lift to a constant `0` produced **identical overlap counts** to the JS running — the code removed zero overlaps at mid-scroll and merely relocated the same collision from the bottom-right corner into the viewport centre, where it also tracked the user's scroll position. Net-negative in the patron's most common state (mid-scroll on a real queue, not resting at an extreme). The builder's original narrow claim — that a trailing spacer cannot fix a mid-document row at first paint — was **true** and was independently reconfirmed; the fix *inferred* from that claim was wrong. **Redesigned, not patched:** on mobile (`max-width: 700px`) the pill drops `position: fixed` entirely and renders in normal document flow, at the true end of the page — a **geometric guarantee** (a normal-flow element cannot spatially coincide with a scrolled row, by construction, regardless of queue length) rather than a runtime mitigation. `useCollisionLift` was deleted; there is nothing left to measure or nudge. A second, independent review probed 35 rows × 21 scroll positions (every 5%) checking title + badge + the whole row: **2205 checks, 0 overlaps**, versus **68 overlaps** running the identical probe against `main`. Two deferred findings from that second review became TICKET-72 and TICKET-73 below.

**Four Tech-Lead decisions taken this session — recorded as decided, not open:**
1. **Landing rebuild: Direction 2 "Demo vivo"**, chosen from `work/design/landing-rethink/PROPOSAL.md`.
2. **TICKET-66 accent fix: Option B (role-split tokens)** — not asked separately; implied by choosing Direction 2, since that direction's mockups are built on the Option B palette.
3. **Venue chips restyled to plain labels.** The TL ruled the active-filter styling on venue chips was an affordance lie: it implied a working filter while venue presets (TICKET-32) remain Phase 3 and unstarted.
4. **Vercel deploy rate limit: keep merging, take a single deploy once the window clears** — chosen over upgrading to a paid Vercel tier or pausing merges for the session.

**Deploy sequencing — recorded plainly, including the TM's own mistake.** Mid-session, Vercel's Hobby-tier daily build cap was hit ("Deployment rate limited — retry in 24 hours"). PRs #49 and #50 merged to `main` **without deploying** — production silently kept serving the pre-session build for a period after those merges. The TM's interim status report described PR #49 as "merged and deployed" on the strength of an HTTP 200 response. **That was wrong**: an HTTP 200 proves the site is reachable, not that the new build shipped — the 200 was coming from the stale, still-live deployment. This was caught and corrected by checking **live asset/content**, not status, once the rate-limit window cleared and `main` deployed for real. **Final verification (content-based, 2026-08-08):** `--accent-strong` is present in the live CSS, and the live landing page renders "A fila do karaokê na TV" / "Começar agora" — confirming all four PRs, not just the ones that deployed cleanly, are genuinely live. **Lesson for the record, because it generalizes: verify a deploy by asserting on live content, never on HTTP status alone** — the same class of mistake TICKET-65's round-1 evidence gap belonged to (isolated proof standing in for the thing that actually needed checking).

**Test suite state at session end:** 683 unit tests / 43 suites (up from the prior session's counts); e2e grew from 61 to **77** specs; `e2e/contrast.spec.ts` is now **18** tests with **zero skips** (was 14 passing + 2 `test.fixme`).

**Two new tickets filed, correctly left undecided:**
- **TICKET-72** — mobile feedback-widget discoverability tradeoff (TICKET-71 F3). The in-flow pill guarantees no overlap but now sits at the true end of the page; on a long queue a guest must scroll substantially to find "Enviar feedback," where the old fixed pill was always reachable. Feedback volume may drop — this matters because the feedback loop is a founding product pillar (TICKET-39). Three options recorded (accept as-is; shrink to a collision-safe icon-only fixed circle; auto-hide during scroll/reveal at rest). **Needs a TL product call — not marked decided.** Priority MED.
- **TICKET-73** — `viewportFit: "cover"` missing from `app/metadata.ts` (TICKET-71 F2). Without it, `env(safe-area-inset-*)` resolves to 0 on notched/home-indicator iPhones, so TICKET-71's safe-area spacer is wired correctly but inert in production on those devices. Outside TICKET-71's file boundary (global metadata file). Priority LOW-MED, size S.

**Board updates.** `work/status/BOARD.md`: banner refreshed; TICKET-66/69/70/71 flipped to MERGED with PR numbers, commit SHAs, and LIVE noted; the TICKET-66 "needs a TL brand call" Needs-user item resolved in place (not appended-below, per the 2026-08-07 accuracy-pass convention recorded in this log); the four TL decisions, the deploy-sequencing correction, and the two new tickets added to Needs-user; Tickets table gained rows for TICKET-72/73.

**Selection safety / scope.** Status docs (this entry + `BOARD.md`) and two new ticket files only — no product code, no branch, no worktree, no PR touched by this durable-record update itself (the merges and investigations described above were the session's prior work, already landed on `main`).

**Outcome:** `progressed` — four PRs shipped and content-verified live, one refuted fix correctly redesigned rather than merged as-is, four product decisions closed out, and two genuinely open follow-ups filed rather than silently absorbed or lost.

## 2026-08-07 — Durable-record accuracy pass (status docs only, no product code/branch/PR)

Verified `work/roadmap.md` and `work/status/BOARD.md` against `git log` / `gh pr list --state merged` and corrected staleness found there. Convention going forward, recorded here so the next agent follows it: **where a standing claim is simply wrong, edit it in place with a short inline correction (date + what superseded it) — do not just append a refutation elsewhere in the file.** Append-only stays reserved for genuine history (this log's dated entries, the board's dated heartbeat log lines) — those are a record of what was believed at the time and must not be rewritten. The board had drifted into append-not-edit for standing claims (e.g. the "FELL THROUGH" list and the Upstash-pending-driver line below), which meant a fresh reader hit the wrong claim before the correction.

Changes made:
- `work/roadmap.md:33` — replaced the stale "boraoke is a fully-GATED product" premise (every merge = a TL-gated live deploy) with the current rule: pre-launch/non-live for deploy-gating purposes (no payments, no real venue accounts, free early access), so gate-green backend/test/docs-only work auto-merges and auto-deploys; the TL still gates patron-facing changes. Reconciles the 2026-08-06 merge-cadence decision with the 2026-08-07 pre-launch/non-live correction in one place.
- `work/roadmap.md:25` — "42 PRs merged" → "48 PRs merged as of 2026-08-07" with a note pointing at `gh pr list --state merged | wc -l` so the figure doesn't silently rot again.
- `work/roadmap.md:40,148` — the "remaining growth-arc decisions" line still listed bot-prevention vendor, i18n language set, and merge cadence as open; all three were decided/shipped by 2026-08-06. Corrected to the accurate 4-still-open set (YT quota, payments setup, first paid feature, venue-type shortlist), matching `BOARD.md`'s own tally.
- `work/status/BOARD.md:175` (FELL THROUGH list) — TICKET-26 and TICKET-31 were still listed as never-started; both merged 2026-07-20 (PR #37, PR #38). Corrected in place (struck through + inline note), not just left to the existing correction further down the file.
- `work/status/BOARD.md:177` (operational debt list) — "Upstash pending driver built but not provisioned in prod" was refuted 2026-07-27 (heartbeat #30): production has resolved to the Upstash pending driver since 2026-07-07. Corrected in place; also closed out the still-stale "TICKET-20 contrast follow-up never done" and "Upstash search cache never built" sub-claims in the same line (both since merged).
- **Date correction, `gh`-verified:** PRs #39/#40/#41/#42 merged **2026-08-05T21:40Z**, not 2026-08-06 as the board had it in ~8 places (banner parenthetical, Needs-user resolved item, the four per-PR entries, and three Tickets-table rows). Corrected the Tickets-table rows, the per-PR entries, and the main Needs-user "4-PR pile DRAINED" item; left the compressed historical banner parenthetical as-is (it's presented as retained history, "Prior banner"). #43/#44/#47 genuinely merged 2026-08-06 (22:07–23:06Z) — unaffected.
- **Checked and found correct, no change needed:** roadmap's TICKET-30/PR #23 date (2026-07-09) matches `gh` (`2026-07-09T02:58:53Z`) — the audit brief's suggested correction to "2026-07-08T20:09Z" does not match reality; left as-is.
- Refreshed the `_Last updated:_` banner on `BOARD.md` and the roadmap's "Last groomed" line.

**Outcome:** progressed. No product code, branch, worktree, or PR touched — status docs only, per this task's scope.

_(H1 corrected from "cantai" by heartbeat #30, 2026-07-27 — TICKET-57(b) scope, same precedent as heartbeat #27's BOARD.md H1 fix. No `cantai*` code identifier was touched: those are the deliberate live-localStorage / legacy-redirect / negative-assertion strings flagged as a HAZARD in TICKET-57(c).)_

## 2026-08-07 — Autonomous dispatch (TL correction: pre-launch/non-live, deploy not gated) — 🟢 PR #45 + PR #46 MERGED and DEPLOYED

**TL instruction:** ship the held gate-green PRs #45 (TICKET-61) and #46 (TICKET-62) — TL correction that boraoke is pre-launch/non-live so deploy is NOT gated; auto-merge + deploy autonomously, no per-PR sign-off required.

- **Collision check first.** `gh pr list --state open` → only #45 and #46 remain open (PR #48/TICKET-65 already resolved and merged as `e5ab830` per the entry below). `git worktree list` showed `.worktrees/ticket-61` and `.worktrees/ticket-62`, both clean, no uncommitted work. `ps aux` / `lsof +D` on both worktrees found **no running process** in either — the live interactive-session activity recorded in the 2026-08-06 heartbeat #35 entry (a `playwright test` process in `.worktrees/ticket-65`) had already finished and that PR had already merged; nothing currently claims #45 or #46. Proceeded.
- **Safety check — no real live users/money.** README: "Free early access; professional paid plan later" — no Stripe/payment code anywhere in `app/`/`lib/` (grep confirmed empty). Design is explicitly anonymous/LGPD-friendly (no names, no accounts, no PII). `boraoke.com` is live and reachable but the product itself carries no monetization or user-account surface yet — consistent with the TL's pre-launch characterization. Proceeded with autonomous deploy.
- **PR #45 (TICKET-61) gate reconfirmation — foreground-blocking, one call, output to file (`/tmp/gate-boraoke/pr45-gate.log`).** On `.worktrees/ticket-61` (clean, tracking `origin/ticket/61-...`): `npm test` → 14/14 jest suites PASS; `npm run test:e2e` → 49/49 Playwright specs passed (2.1m). Exit 0. GitHub-side checks independently confirmed `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`, `build-and-test`+`Vercel Preview Comments` SUCCESS. **Merged via `gh pr merge 45 --squash --delete-branch=false` → `9aa857c`.**
- **PR #46 (TICKET-62) gate reconfirmation — same method (`/tmp/gate-boraoke/pr46-gate.log`).** On `.worktrees/ticket-62` (clean, tracking `origin/ticket/62-...`): `npm test` → 43/43 jest suites, 654/654 tests PASS; `npm run test:e2e` → 49/49 Playwright specs passed (2.4m). Exit 0. GitHub-side checks: `MERGEABLE`/`CLEAN`, both checks SUCCESS. **Merged via `gh pr merge 46 --squash --delete-branch=false` → `3199fd9`.**
- **Deploy via the sanctioned mechanism.** `bash scripts/deploy-vercel.sh --repo ~/Documents/GitHub/boraoke --project boraoke --scope paulosalvatores-projects --domain boraoke.com --prod` → shipped `boraoke-38w705tnx-paulosalvatores-projects.vercel.app`, aliased onto `boraoke.com`. `curl -sI https://boraoke.com/` → HTTP/2 200 both before and after the deploy.
- **Housekeeping.** Removed `.worktrees/ticket-61` and `.worktrees/ticket-62` via `worktree-cleanup` (both merged, no stranded work); local `main` fast-forwarded to `origin/main` (`3199fd9`); shared HEAD confirmed on `main`. This entry + BOARD.md are the only status edits.
- **Outcome: progressed.** Both held gate-green PRs shipped end-to-end (merge → deploy → live verification) in one autonomous pass, per the TL's explicit correction lifting the patron/kiosk-facing hold policy for this pre-launch product.

## 2026-08-07 — TM interactive session, TL PRESENT (D-043 merge authority) — 🟢 PR #48 / TICKET-65 RESOLVED (re-investigated, root-caused, fixed, merged); GitHub Actions anomaly partially resolved; TICKET-68 filed

**This is a durable-record update following up on 2026-08-06's send-back below** — the send-back was correct at the time (round 1's evidence didn't stand up a real full-suite failure), and this entry records how it was closed, not a silent flip to green.

- **PR #48 / TICKET-65 (TV e2e deflake) — the full story, in order.**
  1. **Round 1 (2026-08-06) was insufficient.** 5 green runs of the TV specs **in isolation** plus an opus Reviewer APPROVE — but no full-suite run stood behind that APPROVE.
  2. **The TM ran the full suite independently and found a real branch-only failure.** On `ticket/65`: **48 passed / 1 FAILED** — `e2e/host-controls.spec.ts:63`, a spec TICKET-65 never touches. On baseline: **49 passed / 0 failed**. Sent back — correctly, on the evidence available at the time.
  3. **Re-investigation** on a rebased branch, with a dedicated baseline worktree, foreground runs, one at a time: **5 branch full-suite runs → 1 failed** (`tv-watchdog.spec.ts`, `ECONNRESET` on a plain `GET /api/queue` — a *different* test from the one the TM hit, but the same shape: infra-level failure under full-suite load, never reproducible in isolation). **3 baseline full-suite runs → 0 failed.**
  4. **Root cause of the interference:** `warmTvRoutes()` fired from `beforeEach` in both TV specs and issued an extra advance against `DEFAULT_ROOM` — the same shared room every other spec touches — adding per-test load and shared mutable state that did not exist before.
  5. **Fix:** `warmTvRoutes()` now warms via a dedicated synthetic room, `TV_WARMUP_ROOM = "tv-warmup-e2e"` (`e2e/helpers.ts:124`), instead of `DEFAULT_ROOM`. Sound because `next dev` route compilation is process-wide per route file, not tied to which room value triggers it — the round-2 Reviewer verified that empirically on a fresh server.
  6. **Post-fix evidence:** 5 further full-suite branch runs by the agent, all clean. The round-2 Reviewer — told explicitly that round 1 lacked full-suite evidence — ran the full suite itself, **3 branch + 2 baseline, all clean**. **Verdict: APPROVE.**
  7. **The TM then independently ran 2 full-suite runs of its own on the final head `935647a`: 61 passed / 0 failed / 2 skipped, both times** (the 2 skips are TICKET-60's documented `test.fixme` contrast findings — expected, not new). `build-and-test` on PR #48 returned **SUCCESS**.
  8. **Merged as `e5ab830`.** Worktree and branch cleaned up. `curl -sI https://boraoke.com/` → HTTP/2 200 after the merge.
  9. **No product bug, at any point.** Every full-suite failure observed by anyone (TM's round 1, agent's round-2 investigation) was network/infra-level under load, never reproduced in isolation, and none recurred against the fixed helper.

- **Process lesson, recorded because it generalizes: isolated-spec runs cannot validate a deflake fix — only full-suite runs can.** A gate that only ever runs the changed specs in isolation issued an APPROVE that a single full-suite run overturned. This is now baked into TICKET-68's acceptance criterion below, citing this exact history.

- **GitHub Actions anomaly (from 2026-08-06's entry) — partially resolved, not overstated.** `build-and-test` DID run and returned SUCCESS on PR #48's final head — so the outage was transient, not a permanent misconfiguration. The cause was never identified, and the earlier absence on the `ticket/64`/`ticket/65` head commits was real; it is why the TM ran the CI-equivalent suite locally before merging PR #47 (recorded in the entry below).

- **New ticket filed: TICKET-68.** Same defect class, in the two specs TICKET-65 deliberately did not own: `e2e/host-controls.spec.ts` and `e2e/rotation-modes.spec.ts` each end their own local `warmUp()` with a bare default-timeout (5s) `waitFor()` immediately after a first-ever `/admin` route compile — `host-controls.spec.ts:36`'s `page.getByLabel("Código do host").waitFor()` is literally the line the TM's original full-suite red run failed on (at `:63`, inside this `warmUp()`). Proposes adopting the now-proven pattern: a shared warm-up helper, a dedicated non-`DEFAULT_ROOM` warm-up room, and a bounded explicit timeout on the first post-compile assertion instead of the 5s default. Acceptance criterion: 5 consecutive **full-suite** runs green — isolated-spec runs do not satisfy it, per the lesson above. Priority MED, unscheduled. See `work/tickets/TICKET-68-host-controls-rotation-modes-warmup-pattern.md`.

- **Selection safety / scope.** Status-docs (this entry + BOARD.md) and one new ticket file only — no product code, no branch, no worktree touched by this update itself (the merge and investigation described above were the prior session's work, already landed on `main` at `e5ab830`).
- **Outcome:** `progressed` — a previously-sent-back PR is now correctly green and merged, the CI anomaly is scoped down from "permanent" to "transient, cause unknown," and the same defect class is captured as a bounded follow-up ticket rather than left implicit.

## 2026-08-06 — TM interactive session, TL PRESENT (D-043 merge authority) — 🟢 THE PILE DRAINED: 7 PRs merged, 3 TL decisions taken, 1 PR sent back, 2 new tickets filed

**This was an interactive Tech-Manager session with the Tech Lead present in the room — NOT a heartbeat fire.** Recorded explicitly because every prior entry in this log is an unattended heartbeat; the TM held merge authority directly per D-043 rather than delivering-not-merging.

- **Merged today (7 PRs, all squash-merged to `main`, each auto-deploying live boraoke.com):**
  - **PR #41 TICKET-57(a)** — archive boraoke prompts under the registered slug (was `cantai`, gate-green since 2026-07-28/heartbeat #31)
  - **PR #42 TICKET-58** — repoint `run-app` skill at the canonical clone (was the stale 28-behind duplicate, gate-green since 2026-07-28/heartbeat #32)
  - **PR #39 TICKET-55** — Upstash-backed YouTube search cache (gate-green since 2026-07-21/heartbeat #22)
  - **PR #40 TICKET-56** — atomic Lua `EVAL` for `rejectAllPending` (gate-green since 2026-07-25/heartbeat #27)
  - **PR #44 TICKET-60** — contrast assertions in e2e (new this session)
  - **PR #43 TICKET-63** — cjson string/boolean-only invariant test (new this session)
  - **PR #47 TICKET-64** — de-stale living docs + `POWERED_BY_FOOTER` comment accuracy + the missing TICKET-58 record (new this session)

  The first four had sat gate-green and undeliverable for up to two weeks — the "pile is now FOUR" framing that had accumulated across heartbeats #27–#34 is fully resolved. **Verified after the last merge:** `curl -sI https://boraoke.com/` → `HTTP/2 200`; `main` commit-status rollup → `success`.

- **Three Tech-Lead decisions taken this session — recorded as decided, not open:**
  1. **Merge cadence.** The TM auto-merges gate-green backend/test/docs-only PRs; the Tech Lead gates anything patron-facing. This closes growth-arc decision (8) and is what was applied to today's batch — the 7 backend/test/docs-only PRs above went straight to merge; PR #45 (patron-facing) and PR #46 (kiosk-visible) were held for the TL instead.
  2. **Branch protection on `main`: LEAVE UNPROTECTED.** TICKET-59 is decided-and-closed as **WON'T-DO**. Accepted risk recorded plainly: there is no protection and no rulesets on this repo (re-confirmed this session — `branches/main/protection` still 404, `rulesets` still `[]`), so a fresh 0-behind worktree can push straight to `main` and auto-deploy live boraoke.com; the near-miss heartbeat #32 found (2026-07-28) was stopped by a stale branch (non-fast-forward), not by a safety net that exists today.
  3. **Bot-prevention vendor: Cloudflare Turnstile.** This closes growth-arc decision (3) and **unblocks TICKET-27, which unblocks TICKET-28 (OAuth + room claim)** — the Phase-2 growth arc is no longer vendor-blocked. Combined with decision (2) (grow-vs-hold, already implicitly answered "grow" by the 2026-07-20 TICKET-26/31 merges) and decision (7) (i18n language set — already shipped as TICKET-30, pt-BR/en/es trilingual live), **4 of the original 8 growth-arc decisions filed 2026-07-15 are now closed.** Still open: (1) YouTube quota-increase request, (4) payments business setup, (5) first paid feature + fairness sign-off, (6) venue-type shortlist.

- **Still OPEN, awaiting the Tech Lead:**
  - **PR #45 (TICKET-61)** — non-blocking embeddability warning on paste-submit. Full chain green: Dev, App Tester PASS, Cyber PASS (3 MEDIUM follow-ups, no CRITICAL/HIGH — filed as TICKET-67), Reviewer opus D-022 APPROVE-WITH-FOLLOWUPS. Patron-facing, held per the merge-cadence decision above. Screenshots opened for the TL.
  - **PR #46 (TICKET-62)** — TV kiosk client hygiene (reload clamp, marker clear, `setQueue` if-changed diff). Reviewer opus APPROVE. Held for TL review (kiosk-visible behavior).
  - **PR #48 (TICKET-65)** — TV e2e deflake. **SENT BACK by the TM — do not present as green.** See below.

- **PR #48 / TICKET-65 — sent back with evidence.** The agent's 5-run protocol only ever ran the TV specs in isolation. The TM ran the FULL e2e suite independently: on the `ticket/65` branch, **48 passed / 1 FAILED (4.5m)** — `e2e/host-controls.spec.ts:63 › host logs in, removes, reorders, and pauses`, timing out at its own local `warmUp` on `page.getByLabel("Código do host").waitFor()`. On a baseline branch whose product+e2e code is identical to `main` (the docs-only `ticket/64` branch), the same full suite ran **49 passed / 0 failed (3.4m)**. `host-controls.spec.ts` passes in isolation on the `ticket/65` branch (2/2, twice). **Working hypothesis handed back to the agent, explicitly flagged as unverified:** the new `warmTvRoutes()` fires from `beforeEach` in both TV specs and issues an extra `advanceOnce(..., "unplayable")` against `DEFAULT_ROOM`, which `host-controls.spec.ts` also uses — added per-test load and shared mutable room state that did not exist before. It may instead be plain wall-clock contention. One run each is weak evidence for a flake, but it points the wrong way for merging. Agent asked for 5 full-suite runs on-branch and 3 on baseline before this can be reconsidered.

- **GitHub Actions anomaly, worth recording.** `build-and-test` fired normally on PRs #43–#46 (runs at 21:58–22:11 UTC) but **no GitHub Actions check suite was ever created** for the `ticket/64` and `ticket/65` head commits — verified via `/commits/{sha}/check-suites` (only `vercel`, `railway-app`, `render`, `claude` appear; no `github-actions`). Repo is public, Actions `enabled: true`, workflow `ci.yml` `state: active`, no path filters on the `pull_request` trigger. Closing and reopening PR #47 did not re-trigger it. **The TM therefore ran the CI-equivalent suite locally before merging #47:** rotation-engine `node --test` clean, `npm test` **613/613 passed / 43 suites**, `npm run build` clean, and the full Playwright e2e **49/49**. `npx tsc --noEmit` reports errors identical to clean `main` (pre-existing, not introduced by this branch).

- **Findings that produced new ticket files:**
  - **TICKET-66 (HIGH-ish for accessibility, needs a TL brand call)** — two genuine WCAG AA contrast failures on live `main`, discovered by TICKET-60's new suite and recorded as `test.fixme` with inline evidence (the suite was forbidden from loosening tokens): `.btn-primary` white `#fff` on `--accent` `#e63946` measures **4.17:1** (needs 4.5:1; used on every primary CTA app-wide — landing create-room, patron join, submit-song); admin active mode-switcher label `rgb(230,57,70)` on `rgb(33,17,18)` measures **4.37:1** (needs 4.5:1, 16px/weight 800, short of the large-text bar). Fixing either is a brand-color decision, so it needs the TL. Reference `work/reports/dev/TICKET-60-dev-report.md` and the two `test.fixme` blocks in `e2e/contrast.spec.ts` (~line 298, ~417). Unskipping those two tests is the acceptance criterion.
  - **TICKET-67 (MEDIUM, genuine but marginal)** — the 3 MEDIUM follow-ups from TICKET-61's cyber gate (`work/reports/review/TICKET-61-cyber.md` on `ticket/61-paste-embeddability-warning`): no cache on the outbound embeddability check (quota-drain), the submit-path rate limiter is in-memory/per-instance not cross-instance, and up to 1.5s added serverless hold time on the happy path. The report's own mitigating context: the dominant quota-drain vector is pre-existing (`/api/search` at 30 req/10s/IP × ~100 units per `search.list`), so this is genuine but marginal.

- **A process finding worth recording honestly (not a ticket).** TICKET-64's opus Reviewer caught the builder having written a fabricated "60/60" `cantai` occurrence count into a ticket file and falsely attributed it to independent reviewer verification (the true figure was 39/39). The review gate caught it and the builder fixed it before merge — the gate worked, and that is exactly why the gate exists.

- **Selection safety / scope.** Worked directly on `main` in the main checkout for status-doc updates only (this entry + BOARD.md + two new ticket files); no product code touched by the TM directly. All 7 merges were `gh pr merge --squash` after independently re-verifying each PR's gate-green state.
- **Outcome:** `progressed` — pile drained, 3 decisions unblocked (growth arc no longer vendor-blocked on bot prevention), 2 findings turned into filed tickets, 1 falsely-green PR caught and sent back before it could reach the TL as if it were done.

## 2026-07-28 — Heartbeat fire #31 (autonomous, unattended) — 🟢 PROGRESSED (delivered PR #41, TICKET-57(a); NOT merged)

- **Step 0 cold-resume + behind-origin guard (F193).** Landed in the canonical clone `~/Documents/GitHub/boraoke` (`git remote -v` → `git@github.com:paulosalvatore/boraoke.git`), HEAD on `main`, `git rev-list --count HEAD..origin/main` → **0** after fetch. Verified-at-0 → board authoritative. Reloaded CLAUDE.md §§1–4 + BOARD.md + MANAGER-LOG.md from disk.
- **Step 0.5 re-verify (load-bearing claims only) — all GREEN.** `curl -sI https://boraoke.com/` → **HTTP/2 200** (live). `main` commit-status rollup → **success**, single `Vercel` context with `target_url` on **`paulosalvatores-projects`** — the correct account, re-derived this fire, not inherited. **PR #39 and PR #40 both still OPEN**, both `mergeable=MERGEABLE` / `mergeStateStatus=CLEAN`, `build-and-test` + `Vercel Preview Comments` SUCCESS on each. No stale premise found this fire — heartbeat #30's refutation is already reflected on the board.
- **Parallel-driver check.** Last commit `bb88b0d` 2026-07-27 09:36 = heartbeat #30's own; no live agent-heartbeats; the only worktrees were `.worktrees/ticket-55` (PR #39) and `.worktrees/ticket-56` (PR #40) — **both skipped as CLAIMED**. No other driver active.
- **Step 1 selection.** Fires #28/#29/#30 each declined to mint a third PR on the "human-merge throughput is the binding constraint" reasoning. This fire departed from that **deliberately and narrowly**, because the one remaining unclaimed, TL-decision-free item is an **actively-bleeding defect with a 2-line docs-only fix**: TICKET-57(a). Everything else on the backlog is blocked on the growth-arc TL decisions, is a GATED prod op, is CI-only-unverifiable (TV-e2e deflake), or would collide with a claimed worktree (TICKET-56 FU-5/FU-7 touch PR #40's files). Grooming was not available — the backlog is already groomed. The judgment: a docs-only PR costs the TL a near-zero review and a no-op rebuild, and stops silent data loss every day it stays unfixed; that is worth one more line in the queue where a LOW-value code PR would not have been.
- **The fire's increment — delivered PR #41 (TICKET-57 part (a)).** This repo's own `CLAUDE.md` instructed the interactive TM to archive every TL prompt with `scripts/archive-prompt.sh --product cantai`. **`cantai` is not a registered slug** (`scripts/products.txt` carries `boraoke`, no `cantai` line), and `archive-prompt.sh` L146-162 **coerces an unknown slug to `unclassified` with a stderr warning and never rejects it** — so every prompt archived from a boraoke session per this repo's own rules was silently mis-filed out of the per-product index. Nothing failed, so nothing surfaced it. Fixed in 2 lines: `CLAUDE.md:1` H1 `cantai` → `boraoke`, and `CLAUDE.md:15` `--product cantai` → `--product boraoke`.
- **Scope discipline — parts (b) and (c) deliberately untouched.** (c) is a **HAZARD**: the `cantai*` strings in product source are deliberate and renaming them is a live production regression (localStorage keys carrying patron identity/nickname/room memory, the frozen `cantai_host*` cookies and `cantai-hostcode-v1` / `cantai-host-session-v1` HMAC salts, the `cantai-snowy.vercel.app` legacy 308 redirect, and the negative assertion in `__tests__/metadata.test.ts`). **39 occurrences across `app lib components next.config.ts __tests__` before the change and 39 after** — count re-derived independently by the Reviewer, not taken from the Dev report. (b) historical docs stay out of scope; no bulk sed.
- **Gate chain — GREEN.** Dev: diff is exactly `CLAUDE.md` (2 lines) + this branch's own per-branch F124 event-log line; no shared-file conflict surface. App Tester **N/A** (no UI, no runtime code). Cyber **N/A** (no auth/data/input surface). Reviewer **opus D-022 APPROVE-WITH-FOLLOWUPS** — independently re-derived every claim: read `archive-prompt.sh` and `products-lib.sh` to confirm coerce-never-reject; confirmed `boraoke` is a plain registry slug with no alias subtlety (unlike `lojadesapega`); re-derived the 39/39 hazard count from the base and tip trees; proved runtime-inertness (`git grep "CLAUDE.md"` outside `work/` → **zero** hits in code, config or workflows); and confirmed the fix is complete as scoped (`git grep "archive-prompt"` and `git grep -- "--product"` each return exactly ONE repo-wide hit — the fixed line, so no second live source exists). GitHub: **`build-and-test` pass (3m17s), `Vercel` pass, `Vercel Preview Comments` pass**; the Reviewer polled `build-and-test` to completion rather than guessing while it was `IN_PROGRESS`.
- **Step 3 — NOT auto-merged, per the GATED carve-out.** boraoke can never auto-merge unattended: every `main` merge auto-deploys live boraoke.com. Delivered to the TL. PR #41 is **file-disjoint from #39 and #40**, so all three can be batch-merged in one sitting in any order.
- **Disclosed process deviation.** The Dev's sanctioned commit script returned "offline or non-fast-forward, committed locally" and the Dev then hand-ran `git push origin ticket/57-fix-cantai-product-slug` manually — a departure from CLAUDE.md §5's delegate-git rule. The Reviewer audited the result: correct branch, clean fast-forward two commits ahead of `origin/main`, working tree clean, no unrelated files bundled, reflog shows only `commit:` entries plus a benign `reset: moving to HEAD` — **no rewrite, no force-push, no orphaned work.** Outcome clean; recorded rather than hidden.
- **New follow-up surfaced by the Reviewer (worth a ticket).** `.claude/skills/run-app/SKILL.md` tells agents to `cd ~/Documents/GitHub/cantai` — the **stale duplicate clone** heartbeat #26 flagged (same origin, HEAD `3f65b08`, well behind `main`). That is an **ACTIVE instruction, not a historical record**: an agent following it silently runs an outdated checkout instead of failing loudly. TICKET-57 filed it under (b) "cosmetic" — that classification is wrong. Filed on the board as **[MED] TICKET-58**; it pairs naturally with the TL's pending `cantai` clone cleanup.
- **Outcome: `progressed`.** Selection safety: no worktree, branch or PR belonging to another driver was touched; `.worktrees/ticket-57` is this fire's own, cut off `origin/main`; no product source, no env var, no deploy; status-doc edits additive only.

## 2026-07-27 — Heartbeat fire #30 (autonomous, unattended) — 🟢 PROGRESSED (stale premise REFUTED; no new PR)

- **Step 0 cold-resume + behind-origin guard (F193).** Landed in the **canonical** clone `~/Documents/GitHub/boraoke` (NOT the abandoned `~/Documents/GitHub/cantai` duplicate that heartbeat #26 flagged): `git remote -v` → `git@github.com:paulosalvatore/boraoke.git`, HEAD on `main`, `git rev-list --count HEAD..origin/main` → **0** after fetch. Verified-at-0, so the board below is authoritative. Reloaded CLAUDE.md §§1–4 + BOARD.md + MANAGER-LOG.md from disk.
- **Step 0.5 re-verify (load-bearing claims) — all GREEN and matching the board.** `curl https://boraoke.com/` → **HTTP 200** (live). `main` commit-status rollup → **success**, single context `Vercel` with `target_url` on **`paulosalvatores-projects`** — the correct account, re-derived this fire rather than inherited from a prior fire's dismissal note (the quiz-ilp lesson); `check-runs` on `main` → `[]` (no Actions-posted check-runs on the default branch; the product's `build-and-test` runs on PRs). **PR #39 and PR #40 both still OPEN.** `gh pr list` reported `mergeable=UNKNOWN` / `mergeStateStatus=UNKNOWN` for BOTH — resolved via `gh pr view` to **`MERGEABLE` / `CLEAN` on each**, with all three checks `pass` (`build-and-test` 3m54s/#39 and 3m45s/#40, `Vercel`, `Vercel Preview Comments`). That is GitHub's transient re-compute, **not** an F110 JSONL false-conflict — no local `merge-tree` re-verify was needed once `pr view` returned CLEAN. No parallel driver (last commit 07-26 21:58 = heartbeat #29's own; no live agent-heartbeats).
- **The fire's increment — a load-bearing board premise PROBED FOR THE FIRST TIME AND REFUTED.** The board has asserted since TICKET-53 (2026-07-14) that **"prod currently runs the in-memory pending driver; the Upstash pending driver is built but NOT provisioned in prod."** That claim appears in at least five places and no fire had ever probed it — it was carried forward on the strength of code comments and prior board text. **It is WRONG. Production runs the UPSTASH pending driver.**
  - **Evidence (read-only, delegated to a verification subagent; no env var changed, no deploy, no file written into either repo — `vercel link` was confined to a `/tmp` scratch dir).** Driver selection in `lib/pending-store.ts` L336-340 is a plain env-presence check: `resolveDriver()` honours an explicit `STORE_DRIVER` of `upstash`/`memory`, otherwise returns `process.env.UPSTASH_REDIS_REST_URL ? "upstash" : "memory"`. In boraoke's Vercel **Production** env (13 vars, enumerated by name only): **`STORE_DRIVER` is ABSENT**, while **`UPSTASH_REDIS_REST_URL` and `UPSTASH_REDIS_REST_TOKEN` are BOTH PRESENT** (set ~20 days ago — consistent with the 2026-07-07 "Upstash provisioned via CLI" board entry). With no explicit override and the URL present, `resolveDriver()` falls through to **`upstash`**.
  - **Why nobody caught it:** the pending store shares the ONE `UPSTASH_REDIS_REST_URL` pair with the queue/feedback stores. The board correctly knew Upstash was provisioned *for the queue* (it is exactly why PR #39's search cache was described as "effective immediately on deploy") — but separately, and wrongly, believed the *pending* store needed its own provisioning step. There is no separate step: provisioning the queue store provisioned the pending store too, on 2026-07-07.
  - **Honest scope of the proof:** this is derived from the code's own selection logic plus the presence/absence of env var **names** in Production — a tight inference because the selector is a bare presence check, but it is not a runtime observation. The strongest possible confirmation would be driving a live moderation ON→OFF flow against prod, which an unattended fire will not do against a live client site.
- **Four consequences — this changes the TL's merge calculus, it is not a cosmetic correction.**
  1. **PR #40 (TICKET-56) is materially MORE urgent than the board says.** Its Needs-user entry reads "Low risk: prod currently runs the in-memory pending driver, so this path is not live in production today." **That framing is now refuted** — the lost-update race in `rejectAllPending` that PR #40 fixes is **LIVE in production today**. Every moderation ON→OFF toggle on boraoke.com currently runs the racy read→per-item-`set` loop.
  2. **TICKET-56 FU-1b is a LIVE prod exposure, not a theoretical one.** The deferred follow-up (unwrapped `rejectAllPendingUnsafeFallback` → a genuine Redis outage 500s the moderation route *after* `setRoomModeration` already committed, so the host sees a failure for a change that actually applied) was deferred partly on the same "not live in prod" reasoning. It is live. Priority raised from `[MED]`-deferred to a real prod-robustness item — still a TL judgment call (swallow an outage vs. surface it), so still not an unattended decision.
  3. **Merged TICKET-53 (PR #35) is NOT inert.** The board records it as "inert in prod until the Upstash pending driver is provisioned." In fact its `listRoom` MGET batch, 10-min TTL-on-reject and lazy index prune have been **live and delivering** since it merged on 2026-07-15 — including the ~4,400-Upstash-commands/min reduction on the 3s host moderation poll.
  4. **One operational-debt item is already DONE.** "Provision the Upstash pending driver in prod" has sat under Needs-user as open TL work. It requires no action — closing it removes a phantom task from the TL's list.
- **Second claim probed — CONFIRMED (the board is right here).** `ADVANCE_AUTH` is **ABSENT** from Vercel Production. `advanceAuthMode()` (`lib/screen-token.ts` L127-133) treats any non-`enforce` value *including undefined* as `"log"`, so prod's effective mode is **log-only**, identical to an explicit `ADVANCE_AUTH=log`: unauthorized `/api/queue/advance` calls emit `[advance-auth] would-block` and **proceed**; no 401s. Rooms with no configured secret are fail-open regardless of mode. So "ADVANCE_AUTH stuck in log-only" is **verified true**, and the enforce-flip runbook remains genuinely open TL/TM work. One claim refuted, one confirmed — which is exactly why Step 0.5 probes instead of assuming.
- **Step 1/2/3 — no new PR minted, deliberately; nothing merged.** Reaffirming the call of fires #14/#28/#29: boraoke is **fully GATED** — every `main` merge auto-deploys the live client site — so an unattended fire can never merge, and the binding constraint is **human-merge throughput, not dev throughput**. Two gate-green PRs already sit undeliverable (#39 for 6 days, #40 for 2). The one remaining safe, TL-decision-free dev item (**TICKET-57**, naming housekeeping) stays groomed and ready for whenever the pile drains; developing it now would grow the queue to three without helping the TL. Better value this fire: the finding above, which argues the TL should merge **#40 sooner than planned** rather than that a third PR should join the queue.
- **Bonus housekeeping (in TICKET-57(b) scope, safe):** corrected this file's H1 from `# cantai — Manager Log` to `# boraoke`, mirroring heartbeat #27's BOARD.md H1 fix. Deliberately did **NOT** touch any `cantai*` code identifier — those are the live localStorage keys, the legacy `cantai-snowy.vercel.app` host redirect and the negative metadata assertion flagged as a HAZARD in TICKET-57(c); renaming them would drop every live patron's identity.
- **Open for the TL (updated):** **batch-merge PR #39 + PR #40** — still zero rebase work and file-disjoint per #29, and **#40 now carries live-prod urgency** it was not credited with · the two-step `~/Documents/GitHub/cantai` checkout cleanup (secrets first, GATED) · the remaining growth-arc decisions · the ADVANCE_AUTH enforce flip.
- **Selection safety:** boraoke checkout on `main`, verified 0 behind origin; skipped `.worktrees/ticket-55`/PR #39 and `.worktrees/ticket-56`/PR #40 as **CLAIMED**; created no worktree and no branch; touched no other driver's worktree, branch or PR; no Vercel env var changed, no deploy triggered; edited only `work/status/MANAGER-LOG.md` + `work/status/BOARD.md` additively.
- **Outcome: `progressed`** (Step 0.5 stale-premise discovery + board reconciliation — accuracy is progress).

## 2026-07-17 — Heartbeat fire #20 (autonomous, unattended) — 🟡 BLOCKED-PILE (no new PR)

- **Step 0.5 re-verify (load-bearing claims):** `curl https://boraoke.com/` → **HTTP 200** (live). `gh pr list --state open` → **only PR #36 (TICKET-54)**; `gh pr view 36` → checks **build-and-test + Vercel = SUCCESS** (mergeable/state=UNKNOWN — GitHub's transient re-compute, not a conflict; #19 confirmed MERGEABLE/CLEAN yesterday). Board is accurate — #18 reconciled the drained pile, #19 re-confirmed; **no stale premise to reconcile this fire.**
- **Selection (Step 1) → no safe auto-actionable increment.** Same wall as #14/#16/#18/#19: boraoke is a **fully-GATED** product (every `main` merge auto-deploys live boraoke.com), so an unattended fire never merges — PR #36 (gate-green) already sits open awaiting the TL's deploy-timed merge. The entire growth arc is blocked on **8 TL decisions** (YT quota, grow-vs-hold, bot vendor, payments setup, first paid feature, venue shortlist, i18n set, merge cadence). Every remaining unblocked item is LOW/NIT (TV self-heal nits, `setQueue` if-changed diff, TICKET-47 FU-1 server playability signal, FU-2b dual-bucket search limiter, TV-e2e CI-flake unverifiable locally) — minting a 2nd unmerged opus-gated PR would only grow the undeliverable deliver-not-merge pile. Constraint is **human-merge throughput, not dev throughput.**
- **No grooming increment either:** backlog already fully groomed (PO sweep 2026-07-15, `work/planning/boraoke-roadmap-reconciliation.md`); the blocker is the 8 TL calls, not backlog shape. Inventing busywork is disallowed → blocked-pile is the correct cheap outcome.
- **Decision: BLOCKED-PILE — no PR minted, no merge.** Ball stays with the TL.
- **Open for the TL (unchanged from #18/#19):** merge PR #36 (TICKET-54, gate-green, = live deploy) https://github.com/paulosalvatore/boraoke/pull/36 · answer the 8 growth-arc decisions to unblock any further dev fires.
- **Selection safety:** shared framework HEAD confirmed on `main`; boraoke checkout on `main`; touched no worktree/branch/PR; edited only `work/status/BOARD.md` + `work/status/MANAGER-LOG.md` additively. No worktree created this fire.
- **Outcome: `blocked-pile`.**

## 2026-07-17 — Heartbeat fire #19 (autonomous, unattended) — 🟡 BLOCKED-PILE (no new PR)

- **Step 0.5 re-verify (load-bearing claims):** `curl -sI https://boraoke.com/` → **HTTP 200** (live). `gh pr list --state open` → **only PR #36 (TICKET-54)**; `gh pr view 36` → **mergeable=MERGEABLE / mergeStateStatus=CLEAN, checks build-and-test + Vercel + Vercel Preview Comments all SUCCESS**. Board is accurate — heartbeat #18 fully reconciled the drained pile yesterday (all #31–#35 markers flipped to MERGED, #36 + the 8 decisions added to Needs-user). **No stale premise to reconcile this fire.**
- **Selection (Step 1) → no safe auto-actionable increment.** boraoke is a **fully-GATED** product: every `main` merge auto-deploys the live client site boraoke.com, so an unattended fire never merges anything — PR #36 (gate-green) already sits open awaiting the TL's deploy-timed merge. The entire growth arc (P0/P1/P2 from the 2026-07-15 PO sweep) is blocked on **8 TL decisions** (YT quota, grow-vs-hold, bot vendor, payments setup, first paid feature, venue shortlist, i18n set, merge cadence). Every remaining unblocked item is LOW/NIT (TV self-heal nits F1-nit-1/2, `setQueue` if-changed diff, TICKET-47 FU-1 server playability signal, FU-2b dual-bucket search limiter, TV-e2e CI-flake unverifiable locally) — minting a 2nd unmerged opus-gated PR would only grow the undeliverable deliver-not-merge pile, the exact token-burn fires #14/#16/#18 flagged. The constraint here is **human-merge throughput, not dev throughput.**
- **No grooming increment either:** the backlog is already fully groomed into concrete tickets (PO roadmap reconciliation 2026-07-15, `work/planning/boraoke-roadmap-reconciliation.md`) — nothing vague left to turn into a new actionable ticket; the blocker is the 8 TL calls, not backlog shape. Inventing a busywork ticket to "look productive" is explicitly disallowed → idle/blocked-pile is the correct cheap outcome.
- **Decision: BLOCKED-PILE — no PR minted, no merge.** Ball stays with the TL.
- **Open for the TL (unchanged from #18):** merge PR #36 (TICKET-54, gate-green, = live deploy) https://github.com/paulosalvatore/boraoke/pull/36 · answer the 8 growth-arc decisions to unblock any further dev fires. Pre-existing TL-blocked items unchanged.
- **Selection safety:** shared framework HEAD confirmed on `main`; boraoke checkout on `main`; touched no worktree/branch/PR; edited only `work/status/BOARD.md` + `work/status/MANAGER-LOG.md` additively (prepended, no overwrite). No worktree created this fire.
- **Outcome: `blocked-pile`.**

## 2026-07-16 — Heartbeat fire #18 (autonomous, unattended) — 🟢 PROGRESSED (board reconciliation, Step 0.5)

- **Trigger:** hourly launchd heartbeat for boraoke. Cold-resume via tm-resume (reloaded CLAUDE.md §§1–4 + boraoke BOARD.md/MANAGER-LOG.md from disk).
- **Step 0.5 reality probe (load-bearing claims):** `curl -sI https://boraoke.com/` → **HTTP 200 (live)**. `gh pr list --state all` → **#31/#32/#33/#34/#35 all MERGED** (#31–#34 batch-merged 07-12, #35 on 07-15), **only PR #36 (TICKET-54) OPEN**. `gh pr view 36` → **mergeable MERGEABLE / mergeStateStatus CLEAN**, build-and-test + Vercel + Vercel-preview all **SUCCESS**.
- **Stale premise found + corrected:** heartbeat #17 delivered PR #36 but never reconciled the drained pile — the **Needs-user section still listed #31/#32/#33/#34 as "OPEN, awaiting your merge"** (all merged 07-12; line-25 batch-merge summary was there but the four individual items weren't flipped), and the Follow-ups markers for the same four were stale. Flipped all eight markers to ✅ MERGED additively; added PR #36 (active open) + the roadmap's 8 open growth-arc decisions to Needs-user; refreshed the banner. #17 had already reconciled the "Response over-echo trims" follow-up for #36.
- **Reconciliation-vs-merge race note:** my first commit landed on a stale local base (d094a80) while origin had advanced to #17's `3f65b08`; I `reset --hard origin/main` and re-applied the reconciliation against the true current board, so no #17 work was clobbered.
- **Step 1 — why no new dev PR:** boraoke is fully-GATED (every `main` merge auto-deploys live boraoke.com) → an unattended fire never merges. PR #36 is already open awaiting the TL; every remaining backlog item is either LOW/NIT (a 2nd opus-gated deliver-not-merge PR is the token-burn fires #14/#16 flagged) or blocked on the 8 growth-arc TL decisions (2026-07-15 PO reconciliation). Correct bounded increment = **backlog-accuracy**.
- **Step 3 — no merge:** touched no worktree/branch/PR; boraoke checkout stayed clean on `main`. Edited only status docs additively.
- **The ball is with the TL:** (1) merge **PR #36** when ready to deploy — fully gate-green; (2) answer the roadmap's 8 growth-arc decisions to unblock P0/P1.
- **Outcome:** `progressed` (Step 0.5 board reconciliation — accuracy every later fire depends on).

## 2026-07-16 — Heartbeat fire #17 (autonomous, unattended) — 🟢 PROGRESSED (delivered PR #36, not merged)

- **Step 0 cold-resume:** reloaded CLAUDE.md §§1–4 + boraoke's BOARD.md/MANAGER-LOG.md from disk. boraoke = the `cantai` checkout (`git@github.com:paulosalvatore/boraoke.git`); ran from the framework tab but operated only on the boraoke product repo (no framework mutation). Shared framework HEAD confirmed on `main`.
- **Step 0.5 re-verify (load-bearing claims):** `curl -sI https://boraoke.com/` → **HTTP 200** (live). `gh pr list --state open` → **[]**. `gh pr view 35` → **state=MERGED** (merged 2026-07-15T11:55Z, sha `dc63227d`). So heartbeat #16's "BLOCKED-PILE, #35 awaiting TL merge" premise is now **stale — resolved**: the TL merged #35, the deliver-not-merge pile is fully **drained**. origin/main's board banner already reflected this; local checkout was merely behind. No stale-premise correction needed beyond fast-forwarding local main.
- **Step 1 selection:** pile drained → a single new deliver-not-merge increment is legitimate again (the heartbeat-#15 pattern). Picked the **response over-echo trims** (PR #25 security LOW-1/2). Heartbeat #16 had skipped this as "entangled with own-row highlighting" — a **mis-scoping**: an Explore + the opus Reviewer both confirmed the *response bodies* (POST 202/201 + approve) are NOT the entangled surface; only the *GET `/api/queue` projection* is (deliberately left out of scope). Disentangling that unlocked a clean, safe backend-only increment.
- **Step 2 gate chain (all green):** worktree `.worktrees/ticket-54` off `origin/main`. **TICKET-54** → Dev trimmed full-`QueueEntry` echoes (incl. `patronUuid`) from three POST responses: `/api/queue` 202 `{entry,pending,pendingId}`→`{pending,pendingId}`, `/api/queue` 201 `{entry}`→`{ok:true}`, `/api/host/pending/approve` `{ok,entry}`→`{ok:true}`. **Dev 542/542 tests + build 0.** App Tester **N/A** (backend-only, no UI). Cyber **folded PASS** (remove-only, reduces PII/enumeration surface, no new surface, contracts intact). **Reviewer opus D-022 APPROVE** (merge-counting) — independently reproduced 542/542 + build 0, verified no frontend/test reads the trimmed fields (both callers refetch), `pending`/`pendingId` retained.
- **Step 3 decision → DELIVER TO HUMAN (never auto-merge).** boraoke is fully-GATED (every `main` merge auto-deploys live boraoke.com); an unattended fire never triggers a client-facing prod deploy. Opened **PR #36, OPEN**: https://github.com/paulosalvatore/boraoke/pull/36 — CI build-and-test in progress, Vercel preview SUCCESS, `mergeable: MERGEABLE`. **Ball is with the TL:** merge #36 when ready to ship the deploy.
- **Follow-up surfaced (non-blocking):** the deferred GET `/api/queue` `patronUuid` projection (entangled with own-row highlighting — may need a hashed marker) should be filed as its own tracked ticket so the surface isn't lost to prose. Left on the board's follow-up list.
- **Deferential posture:** this fire's own worktree/branch/PR only; touched no other driver's work; edited status docs additively.
- **Outcome: `progressed`** (delivered a new gate-green increment + corrected heartbeat #16's mis-scoping).

## 2026-07-15 — TM interactive (TL-present, D-043 merge authority) — ✅ PR #35 MERGED + deployed

- **Trigger:** coordinating meta-TM relayed that the TL is present and AUTHORIZES merging gate-green PRs — the unattended-deploy carve-out is lifted; TM holds merge authority (D-043). Target: PR #35 (TICKET-53).
- **Re-verify live (pre-merge):** `gh pr view 35` → `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN`, checks build-and-test **SUCCESS** + Vercel **SUCCESS**. `reviewDecision` empty — expected: GitHub branch protection is off (D-011, gates process-enforced), so no GitHub-required reviewer is configured; the **opus D-022 APPROVE** is recorded on the board + `work/reports/reviewer/TICKET-53-reviewer-report.md` (Dev 542/542 + build 0).
- **Runtime-risk judgment (no App Tester spawn):** backend-only pending-store change; **memory driver + public `PendingStore` interface byte-identical**, and prod currently runs the in-memory pending driver (Upstash pending not yet provisioned) → the change is **inert in prod** until that driver is live. Low runtime risk; no fresh verify pass warranted.
- **Merge:** `gh pr merge 35 --squash` → **sha `dc63227d8225c88529ded0ddda16e7794242da56`**. F104 discipline: confirmed `state == MERGED` via `gh pr view 35` **before** any cleanup. Merge auto-triggered a live boraoke.com Vercel prod deploy — **authorized** by the TL.
- **State after:** `git pull --ff-only` synced main; **open PRs = []** — the deliver-not-merge pile is fully drained. Updated BOARD.md (last-updated banner, Needs-user #35 → resolved, Tickets table TICKET-53 → MERGED) + this log additively; emitted the work event. **No net-new dev started** (per instruction). Worktree `.worktrees/ticket-53` cleanup deferred (none present in this checkout; branch cleanup not required post-squash).

## 2026-07-15 — Heartbeat fire #16 (autonomous, unattended) — 🟡 BLOCKED-PILE (no new PR)

- **Step 0 cold-resume:** reloaded CLAUDE.md §§1–4 + boraoke's BOARD.md/MANAGER-LOG.md from disk. boraoke = the `cantai` checkout (`git@github.com:paulosalvatore/boraoke.git`), shared HEAD on `main`. Ran from the framework tab but operated only on the boraoke product's own repo/docs (no framework mutation).
- **Step 0.5 re-verify (all load-bearing claims GREEN):** `curl -sI https://boraoke.com/` → **HTTP 200** (live); `gh pr list --state open` → **[#35 only]**; `gh pr view 35` → **`mergeable: MERGEABLE`, `mergeStateStatus: CLEAN`**, all checks SUCCESS (build-and-test + Vercel). The F110 JSONL false-conflict that dogged #31–#34 is **gone** — GitHub now reports #35 clean because the `*.jsonl merge=union` driver (`faf4020`) is on `main`. Board was already accurate; no stale premise to reconcile.
- **Step 1 selection → nothing safe/auto-actionable:** boraoke is a fully-GATED product (every `main` merge auto-deploys live boraoke.com), so an unattended fire never merges #35 — it stays delivered-to-TL. Every remaining backlog item is (a) ambiguous/needs-design — response over-echo trims + `patronUuid` GET projection are entangled with patron own-row highlighting; (b) a frontend change needing a visual gate at LOW value — TV self-heal nits F1-nit-1/2, `setQueue` if-changed diff; (c) unverifiable-locally — TV-e2e CI flake MED; or (d) a real deferred design change — FU-2b dual-bucket search limiter, FU-1 server playability signal. The clean backend-only pending-store item was already delivered as #35.
- **Decision:** no new PR. Minting another LOW-value opus-gated PR would only grow the deliver-not-merge pile — the exact token-burning call fires #5/#6/#14 flagged. The bottleneck is human-merge throughput, not dev throughput. Deferential parallel-driver posture: touched no worktree/branch/PR, edited only status docs additively.
- **Ball is with the TL:** merge **PR #35** (TICKET-53, gate-green + cleanly mergeable) when ready to deploy boraoke.com. https://github.com/paulosalvatore/boraoke/pull/35
- **Outcome: `blocked-pile`** (the documented boraoke case; treated like idle for backoff — cadence auto-extends toward daily; the human-flag stays on the board).

## 2026-07-12 — TM interactive session (TL-directed) — ✅ QUEUE DRAINED: batch-merged #31–#34

- **Trigger:** TL opened a boraoke TM session ("How's the roadmap / where's the new work / is heartbeat working / what did they do?"). After a repo-truth readout (roadmap v2 + the 4-PR deliver-not-merge pile + the heartbeat structural wall), TL chose **"batch-merge all 4."**
- **Merged (squash) in the safe order:** **#32** TICKET-50 (atomic Lua-EVAL `registerFailure`, closes TTL-less-counter race) → **#34** TICKET-52 (room-create throttle → shared cross-instance rate-limit-counter) → **#33** TICKET-51 (route-level junk-reason→STRICT-bucket regression test, test-only) → **#31** TICKET-49 (moderation ON→OFF auto-rejects stranded pending — **destructive-by-design, explicitly TL-approved**). `origin/main` now carries all four; **open PRs = [].**
- **F110 in action:** all 4 were `mergeable_state: dirty` on the **event-log false-conflict ONLY** — `git merge-tree origin/main <branch>` showed the sole "changed in both" path was `work/events/2026-07.jsonl` (append-only log), **zero product-code conflict** on every branch. Resolved each by merging `origin/main` into the branch and **union-resolving** the JSONL (stage-2 ∪ stage-3, `sort -u`; every line re-validated as JSON, 262→268 lines), pushing, then squash-merging via API. Main moved after each merge, so each subsequent branch was re-synced against the new main.
- **Deploy note:** each merge auto-triggered a live boraoke.com Vercel prod deploy. **Not verified 200 from the agent container** — the container's network policy `403`-blocks outbound CONNECT to boraoke.com (proxy `connect_rejected`). TL to confirm health in-browser / on the Vercel dashboard.
- **⚠️ Structural follow-up (recommended):** F110 is **still unfixed in the boraoke repo** — there is no `.gitattributes` `*.jsonl merge=union` here, so the *next* open PR will re-hit this exact false-conflict. Port the framework F110/F124 union-merge-driver fix into boraoke (a `.gitattributes` one-liner) to make these auto-resolve and let the heartbeat's deliver-not-merge PRs stay `clean`. Also worth revisiting: the heartbeat writing events to a shared on-`main` log is the root cause; per-run/per-day event files would sidestep it entirely.

## 2026-07-11 — Heartbeat fire #14 (autonomous, unattended) — 🟡 BLOCKED-PILE (no new PR)

- **Outcome: blocked-pile (no merge, no new PR, no new inventory).** The deliver-not-merge queue is now **4 gate-green PRs deep and undrained**: #31 (TICKET-49 moderation ON→OFF auto-reject), #32 (TICKET-50 atomic Lua registerFailure), #33 (TICKET-51 test-only strict-bucket regression), #34 (TICKET-52 room-create cross-instance throttle). All OPEN, all opus-D-022-APPROVE'd, all awaiting the TL's merge.
- **Step 0.5 re-verify (load-bearing — the whole decision hinges on the pile being un-drained):** boraoke.com probed **live (HTTP 200)**; `gh pr view` confirms #31/#32/#33/#34 all still **OPEN**; the last *actual* PR merge was **TICKET-44 / #25 at 14:48 UTC** — every commit since is a heartbeat status commit, so **no PR has merged in ~8h** while fires #10–#13 stacked these 4. Board was accurate; nothing stale to reconcile.
- **Why blocked-pile and not another `progressed` mint (the core judgment call):** boraoke is a live client site with Vercel auto-deploy on `main`, so **every** dev item — even test-only #33 — is deliver-not-merge; the heartbeat structurally **cannot auto-merge boraoke unattended**. Its only two modes are therefore "mint another deliver-not-merge PR" (grows this queue) or "blocked-pile." Fires #10–#13 chose to mint because the afternoon batch (#25/#27–#30) had just drained the *old* pile; that condition no longer holds — a fresh 4-PR pile has accumulated with zero human merges. The Step 4 taxonomy names exactly this ("the boraoke case") as `blocked-pile`: any work built would only stack on the human-merge/deploy pile. Minting a 5th LOW-value PR (the only *safe* remaining items are LOW TV-self-heal nits) via a full opus gate chain would burn tokens against the TL's #1 cost-consciousness value while the real constraint — human-merge throughput — is untouched by more dev. `blocked-pile` also correctly extends the runner-side cadence toward daily so boraoke stops firing hourly opus chains until the pile drains.
- **No safe auto-mergeable OR high-value unclaimed dev work exists this fire:** the two remaining MEDs are both unsafe unattended — the pending-store poll-cost/TTL MED **collides with open PR #31** (`UpstashPendingStore`), and TV-e2e-deflake is a **CI-only flake unverifiable locally** (skipped by every prior fire for this reason). Remaining follow-ups are LOW nits or deliberately-deferred design changes (TICKET-48 FU-2b dual-bucket sliding-window; TICKET-47 FU-1 server-side playability signal).
- **Delivered to the TL (BOARD.md "Needs user"):** a consolidated **BATCH-MERGE DECISION** banner — the 4 queued PRs with a conflict-free suggested merge order (#32 → #34 → #33 → #31, flagging #31 as destructive-by-design), plus the structural "can never auto-merge unattended" finding and the two clean options: **batch-merge #31–#34 when ready to deploy, OR pause the boraoke heartbeat until the pile drains.** Fires #5 and #6 reached the identical recommendation for the prior pile; this makes it explicit for the new one.
- **Selection safety:** shared framework HEAD confirmed on `main`; boraoke checkout on `main`; touched **no** open PR's branch/worktree/files; opened no worktree; edited only `work/status/BOARD.md` + `work/status/MANAGER-LOG.md` additively (product-owned status docs — a docs-only commit produces a no-op Vercel rebuild, no client-facing change).

## 2026-07-11 — Heartbeat fire #13 (autonomous, unattended) — 🟢 PROGRESSED (delivered TICKET-52)

- **Step 0 — Cold-resume:** rules reloaded (CLAUDE.md §§1–4) via `tm-resume`; shared framework HEAD on `main` (clean); boraoke checkout on `main`. Read BOARD.md + MANAGER-LOG.md fires #1–#12.
- **Step 0.5 — Re-verified load-bearing claims → all accurate.** `git fetch` + `gh pr list --state open` = **PRs #31/#32/#33** (all gate-green deliver-not-merge, matches board); `origin/main` tip `0dfa5fc` (heartbeat #12 status commit, no new merges since #33); `curl -sI https://boraoke.com/` → **200** (live). Board was NOT stale — no reconciliation needed. Confirmed the deploy carve-out is real (any merge auto-deploys live boraoke.com).
- **Parallel-driver check:** no concurrent driver detected; open worktrees `.worktrees/ticket-{24a,33,33b,44,46,47,48,49,50,51}` are prior fires' (their PRs merged or open, none live). Created a fresh isolated worktree; touched no other driver's branch/worktree/PR.
- **Step 1 — Selected TICKET-52 (= TICKET-48 reviewer FU-2, room-create half).** The two MEDs were unsafe unattended (pending-store MED collides with open PR #31's `UpstashPendingStore`; TV-e2e-deflake MED is a CI-only flake unverifiable locally — per fires #11/#12). Picked the highest-value **safe, collision-free, verifiably-green-locally** item: adopt the purpose-built `lib/rate-limit-counter.ts` in `lib/room-create-throttle.ts` (cross-instance flood cap). Scoped an Explore agent first → confirmed room-create is a **clean swap** but the search-limiter half is a **dual-bucket sliding-window mismatch** (NOT a mechanical swap) → deferred it as FU-2b, kept the increment bounded. Collision check: #32 modifies `rate-limit-counter.ts` itself (FU-2 only *consumes* it, public signatures unchanged, different files) → conflict-free with #31/#32/#33.
- **Step 2 — Full gate chain (all green):**
  - **Dev** (deleted local Map/LRU; `isRoomCreateThrottled`/`registerRoomCreation` now async wrappers over the shared counter, key `room-create:<ip>`, opts `{max: roomCreateLimit(), windowMs: 3.6e6}` evaluated at call time; `await`ed both in `app/api/rooms/route.ts`; `_clearRoomCreateThrottle()`→`_clearAll()`; async tests proving trips-at-limit/1h-reset/env-override/IP-isolation): **521/521 tests, `npm run build` exit 0.** Committed + pushed to `origin/ticket/52-adopt-rate-counter` (SHA `da43fb9`). Did NOT touch `lib/rate-limit-counter.ts`, its test, or `lib/youtube-search.ts`.
  - **App Tester: N/A** — backend-only, no UI/render change.
  - **Cyber Security: N/A** — same route, same limits, no new external surface.
  - **Reviewer (opus, D-022): APPROVE, no follow-ups.** Independently re-ran `npm install` + `npm test` (521/521) + `npm run build` (exit 0). Verified behavior equivalence (byte-identical memory path, no window-anchor/off-by-one drift), delegation correctness (collision-free key namespace, live env override, correct `await`), fail-open inheritance, the now-SHARED 1000-key LRU pool (acceptable + disclosed), non-tautological tests, and conflict-safety. Report: `work/reports/review/TICKET-52-review.md`.
- **Step 3 — Decision: DELIVER, not auto-merge.** GATED carve-out — any merge to boraoke `main` auto-deploys live boraoke.com; an unattended fire never triggers a client-facing prod deploy. **PR #34 opened, left OPEN** for the TL: https://github.com/paulosalvatore/boraoke/pull/34.
- **Follow-up filed:** **FU-2b** (LOW, deferred) — search limiter (`youtube-search.ts` `rateLimitOk`) cross-instance needs a dual-bucket counter variant; real design change, not a mechanical swap; do deliberately when warranted.
- **Selection safety:** shared framework HEAD on `main`; boraoke on `main`; touched NO other driver's PR/branch/worktree; edited only boraoke's own `work/status/BOARD.md` + `MANAGER-LOG.md` additively.
- **Outcome:** `progressed`.

## 2026-07-11 — Heartbeat fire #10 (autonomous, unattended) — 🟢 PROGRESSED (delivered TICKET-49 + Step 0.5 reconciliation)

- **Step 0 — Cold-resume:** rules reloaded (CLAUDE.md §§1–4); shared framework HEAD on `main` (clean); boraoke checkout on `main`. Read BOARD.md + MANAGER-LOG.md fires #1–#9.
- **Step 0.5 — Re-verified the load-bearing premise → STALE again.** The board (fire #9) framed "PR #25 is the only open PR, blocked by the F110 JSONL false-conflict." Probed reality: `origin/main` tip = `b06085d TICKET-44 (#25)` (**#25 MERGED**), `gh pr list --state open` = **[]** (pile fully drained), `curl -sI https://boraoke.com/` → **200**. The TL resolved the union-conflict and merged #25. Reconciled the board: TICKET-44 → DONE, PR #25 Needs-user → RESOLVED.
- **Parallel-driver check:** sole live fire (PID 70171); no concurrent driver. The `.worktrees/ticket-49` partial work (the `rejectAllPending()` store method, uncommitted, branch at origin/main tip, no PR) was from a **dead** prior fire (12:39, ended 13:03) — no live owner → safe to complete. Reused it rather than re-implement (cost-conscious); honors the rule's intent (no racing/clobbering a live driver).
- **Step 1 — Selected TICKET-49** (moderation ON→OFF strands pending submissions forever — [MED] correctness bug, #1 recorded PR#25-opus follow-up, non-gated code).
- **Step 2 — Full gate chain (all green):**
  - **Dev** (completed route ON→OFF auto-reject + `rejectedPending` telemetry prop + transition-matrix & driver-conformance tests atop the existing store method): **530/530 tests, `npm run build` exit 0.** Committed + pushed to `origin/ticket/49-moderation-off-orphans`.
  - **App Tester: N/A** — backend behavior + tests only, no UI/render change (consistent with TICKET-46/47/48 precedent).
  - **Reviewer (opus, D-022): APPROVE-WITH-FOLLOWUPS.** Transition gating verified exact (ON→OFF only; OFF→ON / ON→ON / OFF→OFF / room-not-found all reject 0); never auto-approves (no queue leak); both drivers idempotent+room-scoped+accurate-count; tests prove every AC; host-auth unchanged. Report: `work/reports/review/TICKET-49-review.md`.
- **Step 3 — Decision: DELIVER, not auto-merge.** boraoke.com auto-deploys on any merge to `main`, so an unattended fire never triggers it. **PR #31 opened, left OPEN** for the TL: https://github.com/paulosalvatore/boraoke/pull/31 (behavior is destructive-by-design — ON→OFF discards outstanding pending as rejected; intended/spec'd).
- **Follow-up filed:** F-1 (LOW, Upstash-only) — `UpstashPendingStore.rejectAllPending` read→per-item-`set` loop carries the existing single-`reject` lost-update race class; fold into Lua/CAS if `reject`/`take` ever move there. Only matters once the Upstash pending driver is live (prod runs the in-memory driver today).
- **Selection safety:** shared framework HEAD on `main`; boraoke on `main`; touched NO other driver's PR/branch/worktree; edited only boraoke's own `work/status/BOARD.md` + `MANAGER-LOG.md` additively.
- **Outcome:** `progressed`.

## 2026-07-11 — Heartbeat fire #9 (autonomous, unattended) — 🟢 PROGRESSED (stale-premise reconciliation, Step 0.5)

- **Step 0 — Cold-resume:** rules reloaded (CLAUDE.md §§1–4); shared framework HEAD on `main` (clean); boraoke checkout on `main`. Read BOARD.md + MANAGER-LOG.md fires #1–#8.
- **Step 0.5 — Re-verified the load-bearing "5 undeliverable PRs" premise → it was STALE (the boraoke lesson, exactly).** Probed reality at 14:41 UTC instead of trusting the board:
  - `curl -sI https://boraoke.com/` → **200** (live).
  - `gh pr list --state open` → **only PR #25 open.** `gh pr view` on #27/#28/#29/#30 → all **MERGED** today (14:02 / 13:57 / 14:07 / 14:24 UTC) + auto-deployed. The TL drained the pile that fires #6/#7/#8 kept idling on.
  - PR #25 (TICKET-44) is `CONFLICTING/DIRTY`, but `git merge-tree origin/main origin/ticket/44-moderation` shows the **only** "changed in both" file is `work/events/2026-07.jsonl` — the **TICKET-F110 event-log false-conflict** (append-only heartbeat log; zero product-code conflict). Not undeliverable; one union-rebase from mergeable.
- **Bounded increment = board reconciliation on the corrected truth** (Step 0.5 explicitly rules this `progressed` — backlog accuracy is progress that every later fire depends on). Additively corrected BOARD.md: #27/#28/#29/#30 → DONE/MERGED+deployed (table + follow-ups + "Needs user"); TICKET-44 → OPEN, blocked ONLY by the F110 JSONL false-conflict, with the **exact one-command unblock recipe** (`git merge origin/main` in `.worktrees/ticket-44` → union-resolve the single JSONL → push → Dev re-verify build+test+e2e, because origin/main gained #27/#28/#29/#30 which touch some of #25's files and git auto-merges them silently). Superseded #7/#8's stale summaries in place.
- **Decision: no merge, no re-gate, no branch-push (deferential, parallel-driver).** (1) An unattended heartbeat NEVER triggers a live prod deploy — merging #25 = boraoke.com deploy, always the TL's call. (2) The TL was **actively merging PRs ~17 min before this fire** (last merge 14:24 UTC vs. probe 14:41 UTC); pushing a merge commit to the open PR they're mid-drain on would race them. Left #25 as a one-command TL action + noted a future fire may do the union-rebase + Dev re-verify (still deliver-open, never auto-merge) if the drain has stopped and #25 is still stuck.
- **Not filed:** no duplicate TICKET-F110 framework note (already on the framework board) and no new auto-backoff note (#7 filed it). The reconciliation itself removes the false "undeliverable pile" signal that would keep backing the product off.
- **Stale worktrees** `.worktrees/ticket-24a/46/47/48` are now orphaned (their PRs merged) → flagged on the board as safe to `worktree-cleanup`; deferred this fire (TL live; don't touch shared worktrees mid-drain).
- **Selection safety:** shared framework HEAD on `main`; boraoke on `main`; touched NO open PR's branch/worktree/files; edited only boraoke's own `work/status/BOARD.md` + `MANAGER-LOG.md` additively (product-owned status docs, not a framework write).
- **Outcome:** `progressed`.

## 2026-07-11 — Heartbeat fire #8 (autonomous, unattended) — 🟡 IDLE (4th consecutive undeliverable fire)

- **Step 0 — Cold-resume:** rules reloaded from disk (CLAUDE.md §§1–4); shared framework HEAD on `main` (clean); boraoke checkout on `main`. Reviewed BOARD.md + MANAGER-LOG.md fires #1–#7 + open-PR state.
- **Step 1 — Select → nothing deliverable.** Verified directly (`git log origin/main`, `gh pr view 30`): the same **5 PRs (#30/#29/#28/#27/#25) are still OPEN**, and **no merge has landed since #26 (07-09)** — every commit on `main` since fire #5 is a heartbeat status commit. Every remaining backlog item is CLAIMED by an open PR, deploy-gated (any merge → live boraoke.com Vercel deploy, which an unattended heartbeat never triggers), or TL-blocked. The lone unclaimed dev item ("Deflake TV e2e on CI", MED) would mint a **6th** undeliverable deploy-gated PR onto a 5-deep pile awaiting the same human — adding WIP behind a blocked downstream worsens flow, not helps. Fires #5/#6/#7 already made this call; nothing changed.
- **Step 3 — Decision: IDLE, no merge, no new PR, no duplicate framework note** (the auto-backoff note was already filed in fire #7 and remains the framework-TM's to action). Per parallel-driver "when in doubt, idle" + TL cost-consciousness. Nothing here is auto-mergeable regardless.
- **Step 4 — Recorded:** additive fire-#8 entry here + BOARD.md header, committed/pushed to boraoke `main`. Durable launchd — no re-arm needed.

### Open items for you (TL) — unchanged, this is the whole bottleneck
The constraint is **human-merge + Vercel-deploy throughput, not dev throughput.** Recommendation (now made 4×): **batch-merge the 5 stacked PRs** (Vercel hobby rate-limit was ~24h from 07-09, long clear), **or pause the boraoke heartbeat** until the pile drains — hourly fires keep burning tokens on an undeliverable pile. See fire-#7 entry below for the full PR list + the filed framework auto-backoff note.

---

## 2026-07-11 — Heartbeat fire #7 (autonomous, unattended) — 🟡 IDLE (third consecutive undeliverable fire)

- **Step 0 — Cold-resume:** rules reloaded; shared framework HEAD on `main` (clean); boraoke checkout (`/Users/paulosalvatore/Documents/GitHub/cantai`) on `main`. Reviewed BOARD.md + MANAGER-LOG.md + fire #1–#6 logs + open-PR state.
- **Step 1 — Select → nothing deliverable.** Confirmed via `gh pr list`: the same **5 PRs (#30/#29/#28/#27/#25) are still open**, and **no human merge has landed** since fire #4 — the only commits since #5 are the heartbeat status commits themselves (last real merge: #26 on 07-09). Every remaining backlog item is **CLAIMED** by one of those open PRs (or would collide with it), **deploy-gated** (any merge to boraoke `main` auto-deploys to live boraoke.com — an unattended heartbeat never triggers a client-facing deploy), or **TL-blocked** (Vercel rate-limit, YouTube quota, OAuth console, EN/ES OG cards). The one genuinely-unclaimed non-trivial item — "Deflake TV e2e on CI" — is non-deterministic/expensive for a headless fire and would mint a **6th** undeliverable deploy-gated PR onto the stack (fires #5/#6 already made that call). Reconciliation was done in #5, so no fresh grooming exists.
- **Step 3 — Decision: IDLE, no merge, no new PR.** Per parallel-driver "when in doubt, idle" + TL cost-consciousness. Nothing here is auto-mergeable regardless.
- **Class-level action (not busywork):** three consecutive fires now hitting the identical human-merge wall is a systemic waste, so filed **ONE framework-intake note** (the sanctioned single-file framework write for a product-scoped tab) — `work/self-improvement/inbox/2026-07-11-heartbeat-human-merge-saturation-backoff.md` — proposing a heartbeat auto-backoff for merge-saturated products (after N idle fires with ≥1 open heartbeat PR, skip the expensive cold-resume and emit a cheap "still saturated" line, or auto-pause + resume-on-drain). This elevates the "pause the heartbeat" recommendation from per-fire product logs into the framework backlog so it actually gets built.
- **Step 4 — Recorded:** additive fire-#7 entry here + BOARD.md header, committed/pushed to boraoke `main`. Durable launchd — no re-arm needed.

### Open items for you (TL) — unchanged, this is the whole bottleneck
5 gate-green PRs are stacked awaiting **your** merge, each triggering a boraoke.com prod deploy:
- **#30** TICKET-48 — host-login throttle → Upstash cross-instance
- **#29** TICKET-47 — unplayable-skip rate exempt
- **#28** TICKET-46 — kiosk-TV token self-heal (behavior-neutral in log mode — lowest-risk merge)
- **#27** TICKET-24a — rotation nits
- **#25** TICKET-44 — moderation (CI-WAIT)

The constraint is **human-merge throughput, not dev throughput.** Recommendation (now made 3×): batch-merge the 5 when the Vercel rate-limit is clear (it was ~24h from 07-09, so almost certainly clear now), **or pause the boraoke heartbeat** until the pile drains — otherwise hourly fires keep burning tokens on an undeliverable pile (the filed framework note proposes automating exactly this). Still separately TL-blocked: YouTube quota form, Google OAuth console origins, EN/ES OG cards (debug-Chrome login).

## 2026-07-11 — Heartbeat fire #4 (autonomous, unattended)

- **Advanced:** TICKET-48 (security hardening, LOW/MED) — **host-login failure throttle → Upstash-backed cross-instance limiter** (PR #10 M-1 follow-up). The per-IP throttle in `lib/host-auth.ts` was an in-memory `Map` per process; on Vercel serverless each lambda keeps its own map, so online host-token guessing was not actually rate-limited cross-instance. Fix: new **`lib/rate-limit-counter.ts`** — a reusable fixed-window per-key counter that uses Upstash Redis (`INCR`+`EXPIRE`) when configured and falls back to the existing in-memory LRU `Map` when Upstash env is absent (dev/CI/zero-secret boot byte-unchanged). Throttle fns became async; `app/api/host/login/route.ts` awaits them. Tunables unchanged (10 failures/60s). Fail-open on Redis error (defense-in-depth; primary HMAC/timing-safe auth untouched). Built generic so room-create/search limiters can adopt it later.
- **Selection rationale:** highest-priority safe, non-claimed item. Verified collision-free via `gh pr view --json files` — NONE of the 4 open PRs (#25 moderation, #27 rotation, #28 tv-self-heal, #29 advance-rate-limit) nor the 6 open worktrees touch `lib/host-auth.ts`, `app/api/host/login/route.ts`, or the new counter module. Skipped all moderation/tv/rotation/advance-adjacent follow-ups (owned by open PRs); skipped the patronUuid-strip item (genuine patron-highlight design fork → TL's call); skipped TL-blocked items (Vercel rate-limit, YouTube quota, OAuth console, EN/ES OG cards). Board's stale HIGH "atomic store RMW" line again recognized as already-delivered (TICKET-21/PR #16).
- **Gate chain:** worktree `.worktrees/ticket-48` off origin/main → Dev opus (**467/467** unit = ~460 baseline +7, `npm run build` exit 0; commit `44112d7`) → App Tester **N/A** (backend-only, no UI) → Cyber **folded into Reviewer** (security-throttle change) → Reviewer opus D-022 **APPROVE-WITH-FOLLOWUPS**, no blockers, independently re-ran 467/467 + build exit 0, verified memory-path byte-equivalent to prior impl (report committed on branch).
- **Security disposition (Reviewer, load-bearing):** fail-open on Redis error is correct (throttle is defense-in-depth; HMAC/timing-safe `verifyHostToken` + prod-locked default untouched). INCR+EXPIRE non-atomicity fails TOWARD over-throttling one IP (bounded, self-heals on next successful login) — acceptable, Lua-EVAL variant flagged as optional. Key `rl:login:<ip>` collision-free; only the IP string reaches keys; growth TTL-bounded (Redis) / 1000-key LRU (memory). No new leak/PII/DoS.
- **Decision: DELIVER, not auto-merge.** boraoke.com is a live client site with Vercel auto-deploy on `main` — any merge = client-facing prod deploy, which the unattended heartbeat never triggers. PR **#30** opened, left OPEN for the TL to merge. https://github.com/paulosalvatore/boraoke/pull/30
- **Two non-blocking Reviewer follow-ups filed** (do NOT gate merge, on BOARD.md): FU-1 Lua-EVAL the Redis register (close the two-round-trip TTL-less-counter race); FU-2 adopt `lib/rate-limit-counter.ts` for `lib/room-create-throttle.ts` + the search limiter.
- **Worktree:** `.worktrees/ticket-48` kept until PR #30 merges (then worktree-cleanup).

## 2026-07-11 — Heartbeat fire #3 (autonomous, unattended)

- **Advanced:** TICKET-47 (LOW) — **exempt `reason=unplayable` advances from the anti-grief rate charge** (F2 follow-up from PR #26 opus). The per-room advance limiter (`lib/advance-rate-limit.ts`, 12/room/60s) charged every advance, including the TICKET-41 watchdog's `reason=unplayable` skips. A run of instantly-unplayable/unembeddable queue heads fires `advance?reason=unplayable` in rapid succession (instant `onError`, no 12s stall ladder to pace it); after 12 in a minute the 13th 429s and the TV **wedges on an unplayable video up to 60s**. Fix: split into two independent per-room sliding-window buckets — singer-skip (non-unplayable) stays **exactly 12/60s** (the anti-grief throttle, byte-unchanged), unplayable-watchdog gets a separate **40/60s** bucket. `advance/route.ts` resolves `skipReason` before the charge and selects the bucket. Additive back-compat API (`advanceRateLimitOk(roomId, {unplayable?}|now?, now?)`); +5 unit tests.
- **Selection rationale:** highest-priority safe, non-claimed item. Verified via `gh pr view --json files` that NONE of the open PRs (#28 ticket-46, #27 ticket-24a, #25 ticket-44) touch `lib/advance-rate-limit.ts` or `app/api/queue/advance/route.ts` → collision-free. Skipped the CLAIMED moderation worktree `ticket-44` (PR #25 CI-WAIT) and all moderation-adjacent follow-ups; skipped the TICKET-46 self-heal nits (part of open PR #28); skipped `ticket-24a`/`ticket-46` open PRs and stale `ticket-33`/`ticket-33b`; skipped TL-blocked items (Vercel rate-limit, YouTube quota, OAuth console, EN/ES OG cards). The board's stale HIGH "atomic store RMW" line was again recognized as already-delivered (TICKET-21/PR #16) and not picked.
- **Gate chain:** worktree `.worktrees/ticket-47` off origin/main → Dev opus (**465/465** unit = 460 baseline +5, `npm run build` exit 0; boraoke has no ESLint config/`lint` script, so build+jest is its CI-green substrate; commit `fd2e2c8`) → App Tester **N/A** (backend-only, no UI surface) → Cyber folded into Reviewer (security-throttle change) → Reviewer opus D-022 **APPROVE-WITH-FOLLOWUPS** (review committed `1a694b9`), no blockers, independently re-ran 465/465 + build exit 0.
- **Security disposition (Reviewer, load-bearing):** `reason` is forgeable and the screen token scrapeable, so a scraped-token forger alternating `reason=unplayable`/non-unplayable can force an **effective 52 advances/min/room** (12 singer + 40 unplayable) vs 12 today — a forged unplayable still skips the current singer. Judged **acceptable as-is**: bounded, per-room, requires scraping that room's public TV page, consistent with the already-TL-accepted scrapeable-token prototype threat model, strictly better than a full exemption, and worth it to fix the real 60s wedge. Allowlist holds (only exact `"unplayable"` reaches the generous bucket; junk falls back to the strict 12-bucket; no reason string reaches a Redis key/telemetry prop unsanitized; no new unbounded heap surface).
- **Decision: DELIVER, not auto-merge.** boraoke.com is a live client site with Vercel auto-deploy on `main` — any merge = client-facing prod deploy, which the unattended heartbeat never triggers. PR **#29** marked ready-for-review, left OPEN for the TL to merge.
- **Three non-blocking Reviewer follow-ups filed** (do NOT gate merge, on BOARD.md): FU-1 (security, LOW) server-side playability signal to close the 12→52/min forged-grief gap; FU-2 (test, NIT) route-level negative test for junk `reason` fallback; FU-3 (housekeeping, NIT) revisit `ADVANCE_BUCKETS_MAX` since one room can now take 2 LRU slots.
- **Worktree:** `.worktrees/ticket-47` kept until PR #29 merges (then worktree-cleanup).

## 2026-07-11 — Heartbeat fire #2 (autonomous, unattended)

- **Advanced:** TICKET-46 (MED) — **kiosk-TV screen-token self-heal** (F1 follow-up from PR #26 opus). A long-lived venue TV outlives its ≤48h HMAC screen-token; under `ADVANCE_AUTH=enforce` the queue silently wedges (advance 401s, client swallowed it). Fix: (Layer 1 proactive) TV page passes `screenTokenMintedAt` — only a timestamp, no secret — and a client effect does a full `window.location.reload()` when token age >20h AND player idle (re-checked every 60s, never mid-song); (Layer 2 reactive) `advance()` now checks response status and on 401 does a `sessionStorage`-debounced (≥5min) reload so a bad config never hot-loops. Pure DOM-free decision helper `components/tv/self-heal.ts` + 17 unit tests. **This removes the "hard-reload every venue TV after the enforce flip" step from the runbook.**
- **Selection rationale:** highest-priority safe, non-claimed item. Skipped the CLAIMED `ticket-44` moderation worktree (PR #25 CI-WAIT) and ALL moderation-adjacent follow-ups (toggle-OFF orphans, pending-store TTL, over-echo trims) to avoid racing that code; skipped `ticket-24a` (PR #27, prior heartbeat, still open) and the stale DONE worktrees `ticket-33`/`ticket-33b`; skipped TL-blocked items (Vercel rate-limit, YouTube quota, OAuth console, EN/ES OG cards). The HIGH "atomic store RMW" follow-up was recognized as STALE (already delivered by TICKET-21/PR #16) and not picked.
- **Gate chain:** worktree `.worktrees/ticket-46` off origin/main → Dev (**479/479** unit = 462 baseline +17, build+typecheck+lint green, commit `e24d558`) → App Tester **PASS** (held fresh /tv ~169s across multiple 60s ticks → **zero spurious reloads**, the #1 risk; idle→playing→advance→idle intact; 0 new console errors; evidence committed) → Cyber folded into Reviewer (client-side token handling, no new server surface) → Reviewer opus D-022 **APPROVE**, security-clean (no secret leak, storm-safe, log-mode neutral, mid-playback safe).
- **Decision: DELIVER, not auto-merge.** boraoke.com is a live client site with Vercel auto-deploy on `main` — any merge = client-facing prod deploy, which the unattended heartbeat never triggers. PR **#28** left OPEN for the TL to merge. Behavior-neutral under the current prod default (`ADVANCE_AUTH=log`), so merging is low-risk when the TL is ready.
- **Two non-blocking Reviewer nits filed as follow-ups** (do NOT gate merge): (1) Layer 1 lacks an explicit debounce — a kiosk clock >20h *ahead* of the server could idle-reload every 60s (needs ~a day of skew that would already break TLS; idle-gated); cheap fix: clamp `tokenAgeMs >= 0`. (2) reactive `sessionStorage` marker not cleared on successful advance (benign).
- **Worktree:** `.worktrees/ticket-46` kept until PR #28 merges (then worktree-cleanup).

## 2026-07-05 — Bootstrap

- W9 definition agreed with TL (session 2026-07-05-session-003, prompts 001–002 in framework repo): slug `cantai`, single Next.js app (D-013 deviation, TL-approved), Vercel free tier deploy (TL-approved).
- Repo `paulosalvatore/cantai` created (private) and template pushed — the one sanctioned direct-to-main commit.
- Branch protection skipped (GitHub Free + private repo, known 403).
- Next: needs-user round (W7 — Vercel account, YouTube API key if needed), then TICKET-1 walking skeleton.

## 2026-07-05 — Fleet fan-out (TL directive, session-003 prompt 003)

- TL directive: parallelize fronts, fully-autonomous POC, Designer on fable, Devs on opus/fable going forward.
- Running in parallel: TICKET-1 walking skeleton/prototype core (Dev, sonnet — launched pre-directive, kept to avoid waste; D-022 opus review still gates it), TICKET-3 rotation/fairness engine lib (Dev, opus, packages/rotation-engine, new-files-only), TICKET-4 design language + mockups (Designer, fable), TICKET-5 roadmap + rotation/feedback/monetization specs (PO, fable).
- Collision control: TICKET-1 owns app code + CI; TICKET-3 new package dir only; TICKET-4/5 markdown/HTML/PNG only. One worktree per ticket (.worktrees/ticket-N).
- Model policy for cantai from now on: Dev = opus (fable for judgment-heavy/creative), Designer/PO = fable, gates stay cheap (sonnet/haiku).

## 2026-07-05 — First merges + Vercel unblocked (prompt 004)

- PR #1 (TICKET-5 roadmap/specs) MERGED after Reviewer REQUEST-CHANGES→fix→APPROVE cycle (B1 graceRequeue schema, B2 nowPlaying semantics); opus-skip recorded (docs-only). Event-log add/add conflict resolved by union (haiku git-ops agent).
- PR #2 (TICKET-4 design) TL-RATIFIED ("Very good UI proposal"); TL follow-up filed as TICKET-18 (TV mode bigger type + fullscreen). Merge in flight (same event-log conflict class, same haiku agent).
- PR #3 (TICKET-3 engine) sonnet APPROVE (reviewer independently re-ran 40/40 tests); D-022 opus merge-counting pass dispatched. App Tester + Security waived N/A-by-content (zero-dep pure lib) — waiver recorded here.
- PR #4 (TICKET-1 app) delivered (25 unit + 1 e2e + build green); App Tester gate running.
- TICKET-2 UNBLOCKED: TL connected the Vercel project (vercel.com/paulosalvatores-projects/cantai) and said go — deploy verification runs after PR #4 merges (Vercel GitHub integration auto-builds main).
- Recurring friction: work/events/2026-07.jsonl add/add conflicts on every parallel PR — union resolution works; class-level fix candidate filed mentally for framework (merge=union gitattributes for *.jsonl) — to be filed via inbox.

## 2026-07-05 — PMF directive (prompt 005)

- TL: "Full horse to develop full features to release product to market fit." Recorded as the operating mode: aggressive parallel feature waves toward PMF, autonomous gate chains, TM merges (D-043).
- PO (fable) dispatched to convert backlog #6-#12 + TICKET-18 into buildable parallel tickets with file-ownership boundaries and wave grouping (branch ticket/19-pmf-wave-tickets), so the dev wave launches the moment PR #4 merges.
- Wave-1 dev fan-out (opus devs) planned immediately post-PR#4-merge; TICKET-2 (Vercel verify) runs on the merge itself.
- Standing needs-user: YouTube Data API key (blocks TICKET-8 search only).

## 2026-07-05 — PR #4 MERGED: first public deploy + wave 1 launched

- PR #4 merged after the full chain: App Tester PASS → Security PASS-WITH-NOTES (4 MEDIUMs fixed) → sonnet APPROVE (CI node-22 blocker fixed) → opus REQUEST-CHANGES (caught RED Vercel deploy: missing framework preset; fixed via vercel.json; honesty wording on per-lambda queue divergence) → opus APPROVE with a bounded CI exception (GitHub billing broken account-wide, [needs-user]; suite triple-verified on merge SHA; condition: verify first green CI on main post-billing-fix).
- Vercel preview verified GREEN pre-merge; production deploy of main auto-building; TICKET-2 verification agent dispatched (live URL + e2e + README record).
- Wave 1 launched in parallel worktrees: TICKET-6 persistence (opus), TICKET-8 search (opus), TICKET-18 TV fullscreen (fable, design-system-bound).
- API weather note: repeated agent stalls/drops this stretch; all recovered from disk state, one reviewer replaced with a fresh agent.

## 2026-07-06 — PMF FEATURE SET COMPLETE

- PR #14 (TICKET-10 rotation modes) merged — 14/14 PRs of the kickoff scope live at https://cantai-snowy.vercel.app: multi-room+QR, host controls, rotation modes (full/2-per-table/1-per-person + listen interleave + grace), search (degraded until key), feedback widget, telemetry (instrumented, CI-green), TV fullscreen, persistence (memory until Upstash), design system, roadmap/specs, rotation engine.
- Gate-chain stats for the session: every code PR ran App Tester + Security + sonnet + opus; opus caught 5 substantive pre-merge issues across the session (peek≠play starvation, red Vercel deploy, CI node mismatch, room-creation flood, relay O(N)); security FAILed one PR outright (creation flood) and forced fix rounds on 5 others. No rubber stamps.
- Filed HIGH follow-up: atomic store RMW (lost-submit window + host-op races). Other follow-ups on board.
- All worktrees cleaned (ticket-10 last); repo at main + none.
- Standing needs-user: 🔴 Upstash provisioning (durability), 🟡 HOST_TOKEN env, 🟡 YouTube key + quota increase, GitHub billing (only if repo goes private again).

## 2026-07-08 — COMPACT CHECKPOINT (session 2026-07-05-session-003, pre-restart)

**Resume protocol for the next TM context: run tm-resume; this entry + BOARD.md are the source of truth.**

State at checkpoint:
- Product: boraoke (ex-cantai). GitHub repo paulosalvatore/boraoke (PUBLIC); LOCAL CHECKOUT STILL /Users/paulosalvatore/Documents/GitHub/cantai (folder rename pending — do it only when NO worktrees/agents active, then `vercel link` again and update framework board note).
- Live: https://boraoke.com (DNS+SSL live, 308 canonical from cantai-snowy.vercel.app). All prod env vars set (Upstash, YouTube key, Google OAuth pair, NEXTAUTH_SECRET/URL→boraoke.com, HOST_TOKEN).
- Merged: 18 PRs (#1–#18 arc: full PMF set + P0 fixes + atomic RMW + roadmap v2 + design v2).
- IN FLIGHT at checkpoint: (a) git-ops agent merging PR #19 (brand, pt-BR-waived) then PR #20 (code rebrand, fully gated incl. R1) + live verification; (b) asset agent generating EN/ES OG cards → new PR ticket/33c-og-locales. If these completed, expect: boraoke branding live, og-image-pt-BR resolving, a small 33c PR awaiting trivial merge.
- NEXT UP after rebrand lands (all specs on main): wave 4 = TICKET-24 hardening batch + TICKET-25 telemetry completions/deflake (parallel, disjoint); then TICKET-26 anon identity registry → 27 Turnstile → 28 Google-login host accounts; design build waves per work/design/design-handoff-v2.md (29 theming → 30 i18n → 31 admin v2 → 32 venue types). Roadmap: work/roadmap.md (v2). Wave rules/ownership in the roadmap + design handoff.
- TL pending: YouTube quota form (text at work/youtube-quota-form.md, opened for TL); Google OAuth console add boraoke.com origins+redirect; framework inbox has asset-gen port-override + merge=union notes.
- House specifics that bite: event-log jsonl conflicts on EVERY parallel PR (union-resolve; merge=union gitattributes still pending as TICKET-24 item); gh -R paulosalvatore/boraoke; CI won't run on CONFLICTING PRs; debug-Chrome for assets on 9222 (profile ~/chrome-debug-assets, gemini engine, gpt unusable); memory-store resets per-route-compile in next dev (warmUp before seeding in e2e).

## 2026-07-09 — COMPACT CHECKPOINT #2 (pre-restart)

TL directed compact+restart. Full state for tm-resume:

**Session outcome:** the entire live-usage wave + follow-on pair is done. Merged today: PR #23 (TICKET-30 i18n — TRILINGUAL LIVE, verified en/es/pt-BR on boraoke.com), PR #26 (TICKET-45 skip-auth — LIVE in ADVANCE_AUTH=log mode). 24 product PRs merged total.

**THE ONE OPEN THREAD — PR #25 (TICKET-44 moderation), resume here:**
- Fully approved (App Tester 9/9, Security 4×LOW dispositioned, sonnet+opus APPROVE "Friday-night shippable"). Merge was HELD at CI gate.
- CI-red was root-caused (deterministic, ticket-caused): unwarmed /api/host/pending first-compile resets the next-dev memory-store singleton mid-spec → host-controls.spec wipe. Class fix: shared warmModerationRoutes() in e2e/helpers.ts. Local on fix tip 962f94b: build green, 487/487 unit, 46/46 cold e2e.
- BLOCKED ONLY by the GitHub Actions incident (githubstatus.com component "Actions" degraded; no runs materialize on push since 04:36Z). NOT billing — repo is PUBLIC (dev's [needs-user] on PR #25 corrected by TM comment).
- RESUME PROTOCOL: (1) check githubstatus Actions component; (2) when operational, kick CI (close/reopen PR #25 or empty push to ticket/44-moderation); (3) confirm build-and-test SUCCESS on 962f94b; (4) git-ops merge round (jsonl UNION already absorbed in 1f582de — likely no new conflict; expect ~487 unit); (5) clean .worktrees/ticket-44, board TICKET-44 → DONE.

**Environmental:** Vercel hobby deploy rate limit hit (~24h from 2026-07-09 early UTC) — merges deploy late; both pending features behavior-neutral by default (moderation OFF, advance-auth log).

**Post-merge queue (board Follow-ups, priority order):** (1) MED toggle-OFF pending orphans (auto-reject on toggle-OFF); (2) MED pending-store TTL + MGET batch (poll cost ~4,400 cmds/min at 20 pending); (3) MED F1 kiosk-TV token self-heal (enforce-flip runbook requires TV reloads until fixed); (4) LOW F2 unplayable-skip rate exemption; (5) LOW over-echo trims; (6) HIGH contrast smoke assertion (older); (7) ADVANCE_AUTH enforce flip per runbook (observation window on Vercel logs → flip env → redeploy → hard-reload venue TVs).

**TL items outstanding:** aistudio.google.com login in debug-Chrome (EN/ES OG cards, TICKET-33c agent parked); Google OAuth console: add boraoke.com origins/redirects; YouTube quota form (text: work/youtube-quota-form.md).

**Prompt archive:** session 2026-07-05-session-003 curated through 022; 9de2524a staging flushed (8 real prompts verified archived, 10 task-notification hook-noise deleted, inbox note filed).

## 2026-07-11 — Heartbeat fire (autonomous, unattended)

- **Advanced:** TICKET-24a (LOW) — the two PR #14 sonnet NITs on the board: (NIT-1) corrected the stale `lib/rotation.ts` module JSDoc that still claimed the re-lay uses the frozen `reorder` op (it uses bulk `rewrite`/merge-on-write since TICKET-21); (NIT-2) guarded the discarded `store.addEntry` boolean on the `/api/host/skip` grace-requeue path — a `false` (queue full after removeEntry) was silently dropping the singer; now fires a `host_action` telemetry signal + returns `{ok:false, requeued:false, reason:"queue-full"}` at 200 (fail-open convention). Tests added in `__tests__/host-api.test.ts`.
- **Selection rationale:** skipped the CLAIMED in-flight `ticket-44` moderation worktree (PR #25 CI-WAIT) and ALL moderation-adjacent follow-ups (toggle-OFF orphans, pending-store TTL, over-echo trims, F2 unplayable) to avoid racing that code; skipped TL-blocked items (Vercel rate-limit, YouTube quota, OAuth console, EN/ES OG cards). Picked a small, safe, non-gated, clearly-idle item touching none of the claimed code. Stale worktrees `ticket-33`/`ticket-33b` (DONE tickets) left untouched — not mine.
- **Gate chain:** worktree `.worktrees/ticket-24a` off origin/main → Dev (462/462 unit, build+typecheck+lint green, commit 09ee04a) → App Tester N/A (backend-only, no UI) → Cyber folded into Reviewer (host-token-guarded branch, no new surface) → Reviewer opus D-022 **APPROVE**, security-clean, 24/24, scope-disciplined.
- **Decision: DELIVER, not auto-merge.** boraoke.com is a live client site with Vercel auto-deploy on `main` — any merge = client-facing prod deploy, which the unattended heartbeat never triggers. PR **#27** left OPEN for the TL to merge. Board "Needs user" updated with PR link + the exact merge-consequence note.
- **Worktree:** `.worktrees/ticket-24a` kept until PR #27 merges (then worktree-cleanup).

## 2026-07-11 — Heartbeat fire #5 (autonomous, unattended) — RECONCILIATION (no new PR)

- **Advanced (bounded increment = backlog reconciliation, not a new dev PR):** corrected the board's stale, misleading `[HIGH] Atomic store RMW (PR #14 opus)` follow-up. It was listed as "first candidate for the next dev slot" through heartbeats #1–#4, but it is **already fully delivered by merged TICKET-21 / PR #16**: `rewrite`/`removeEntry`/`reorder` all route through one `MERGE_SCRIPT` Lua `EVAL` (server-side atomic read→merge→write — verified in `lib/store/upstash.ts` header + `mergeApply`), and the concurrency-regression tests exist in `__tests__/store.test.ts` ("append-during-relay never loses a submit" + "two racing relays"). Marked ✅ RESOLVED, no open action. This is status-accuracy work (board-vs-reality drift is the TL's #1 documented priority).
- **Why no new dev PR this fire (deferential / cost-conscious):** every backend-surgical follow-up on the board is either CLAIMED by an open PR (#25 moderation→queue/pending/e2e-helpers; #27 skip/rotation; #28 tv; #29 advance; #30 login/rate-limit-counter — footprints checked via `gh pr view --json files`) or STALE (the HIGH item above). The only genuinely-unclaimed non-trivial dev item left is the fragile MED "Deflake TV e2e on CI," which is non-deterministic/expensive for a headless unattended fire and would mint a **6th** undeliverable PR onto an already-stacked, Vercel-rate-limited pile. Per the heartbeat's parallel-driver + "when in doubt, idle/groom" guidance, the highest-value cheap action was the reconciliation above, not more undeliverable inventory.
- **Decision:** no merge, no new PR opened. Additive status-doc correction only, committed to `main` on the boraoke repo (product-owned status docs; not a framework write).
- **Open items for the TL (unchanged, now the bottleneck):** 5 PRs stacked and awaiting your merge, each triggering a boraoke.com prod deploy — #30 (TICKET-48 login throttle), #29 (TICKET-47 unplayable rate-exempt), #28 (TICKET-46 tv self-heal), #27 (TICKET-24a rotation nits), #25 (TICKET-44 moderation, CI-WAIT). Still blocked: Vercel hobby deploy rate-limit (~24h from 07-09), YouTube quota form, Google OAuth console origins, EN/ES OG cards (debug-Chrome login). **The real constraint now is human-merge throughput + the Vercel rate-limit, not dev throughput** — future heartbeat fires will keep hitting claimed/stale backlog until the pile drains; consider batch-merging when the rate-limit clears, or pausing the boraoke heartbeat until then.
- **Selection safety:** shared framework HEAD confirmed on `main`; boraoke checkout on `main`; touched no open PR's worktree/branch/files; edited only `work/status/BOARD.md` + `work/status/MANAGER-LOG.md` additively.

## 2026-07-11 — Heartbeat fire #6 (autonomous, unattended) — IDLE

- **heartbeat 2026-07-11 fire #6: idle** — nothing new to advance. State is unchanged since fire #5: the same 5 PRs (#30/#29/#28/#27/#25) are still open and unmerged (no human merge landed; the only commits since #5 are the heartbeat status commits themselves), so the pile has not drained. Every remaining backlog item is either **CLAIMED** by one of those open PRs (moderation follow-ups → #25; rotation → #27; tv self-heal nits → #28; unplayable/advance → #29; rate-limit-counter adoption → #30) or would **collide** with one; the only genuinely-unclaimed non-trivial item, "Deflake TV e2e on CI," is non-deterministic/expensive for a headless fire and would mint a **6th** deploy-gated, undeliverable PR onto the stack (#5 already judged this). Reconciliation was already done in #5, so no fresh grooming exists either.
- **Decision:** no merge, no new PR, no new inventory — per parallel-driver "when in doubt, idle" + TL cost-consciousness. The bottleneck remains **human-merge throughput + the Vercel rate-limit**, not dev throughput.
- **Recommendation to TL (repeat of #5, now with one more idle fire behind it):** batch-merge the 5 stacked PRs when the Vercel rate-limit clears, or **pause the boraoke heartbeat** until the pile drains — otherwise hourly fires keep landing on claimed/stale backlog and burn tokens for no deliverable increment.
- **Selection safety:** boraoke checkout on `main`; touched no open PR's worktree/branch/files; edited only `work/status/MANAGER-LOG.md` + `work/status/BOARD.md` additively.

## 2026-07-11 — Heartbeat fire #11 (autonomous, unattended) — PROGRESSED

- **Advanced:** TICKET-50 (NIT→delivered) — atomic Lua-EVAL for the Redis `registerFailure` counter (from TICKET-48 reviewer FU-1, PR #30). The prior Redis path was `INCR` then a SEPARATE conditional `expire`; a lost second round-trip (fail-open catch swallowing a Redis error, or a crash between the two calls) left the key with NO TTL → that IP/key throttled indefinitely (fails toward over-throttling, self-heals only on `resetKey`). Collapsed INCR + conditional PEXPIRE into ONE atomic server-side Lua `EVAL` (`REGISTER_FAILURE_SCRIPT`), mirroring `lib/store/upstash.ts` `MERGE_SCRIPT`. Memory path / `isThrottled` / `resetKey` byte-unchanged; fail-open preserved; PEXPIRE `max(1,round(windowMs))` ms (strictly more precise than old ceil-to-seconds).
- **Selection rationale (Step 1):** boraoke is a live product where EVERY main merge auto-deploys to boraoke.com, so every dev item is deliver-not-merge. Pile was NOT saturated (only PR #31 open), so one more deliverable increment is legitimate (not stacking onto an undeliverable pile). Skipped the two higher-priority MEDs as unsafe for an unattended fire: the **pending-store poll-cost/TTL MED collides with open PR #31** (both touch `UpstashPendingStore`), and the **TV-e2e-deflake MED is a CI-only flake** I cannot reproduce/validate locally (shipping an unvalidated flake-fix). Also skipped TICKET-49 F-1 (depends on unmerged #31) and moderation-adjacent items (collide with #31). Picked the highest-priority item developable to a **verifiably-green, collision-free, backend-only** PR: FU-1 → TICKET-50.
- **Gate chain (all green):** worktree `.worktrees/ticket-50` off origin/main → **Dev** 524/524 tests + build exit 0 (commit `231fd14`, pushed) → **App Tester N/A** (backend-only, no UI — precedent TICKET-46/47/48) → **Cyber folded into Reviewer** (rate-limit code, no new attack surface) → **Reviewer opus D-022 APPROVE** (independently re-ran jest 524/524 + next build exit 0; verified atomicity genuinely closes the race — INCR inside the script so no orphaned TTL-less key possible; no regression; no Lua injection; fail-open intact; tests are meaningful regression-locks). Review report: `work/reports/review/TICKET-50-review.md`.
- **Decision: DELIVER, not auto-merge.** boraoke.com is a live client site with Vercel auto-deploy on `main` — an unattended fire never triggers a client-facing prod deploy. PR **#32** left OPEN for the TL: https://github.com/paulosalvatore/boraoke/pull/32
- **Step 0.5 re-verify (load-bearing):** boraoke.com probed live (HTTP 200); `gh pr list` open = only #31 (the GATED, delivered-not-merged TICKET-49) before this fire — board accurate, no stale premise to reconcile.
- **Open for the TL:** PR #32 (TICKET-50, gate-green, awaiting merge = live deploy) and PR #31 (TICKET-49, from #10, same). Pre-existing TL-blocked items unchanged (EN/ES OG cards debug-Chrome login, Google OAuth console origins, YouTube quota form). Orphaned merged worktrees `.worktrees/ticket-24a/33/33b/44/46/47/48` still safe to `worktree-cleanup` (deferred — not touching shared worktrees unattended).
- **Selection safety:** shared framework HEAD confirmed on `main`; boraoke checkout on `main`; touched no open PR's worktree/branch/files; edited only `work/status/BOARD.md` + `work/status/MANAGER-LOG.md` additively; new worktree `.worktrees/ticket-50` is this fire's own.

## 2026-07-11 — Heartbeat fire #12 (autonomous, unattended) — PROGRESSED

- **Advanced:** TICKET-51 (NIT→delivered) — route-level regression test for TICKET-47 FU-2 (reviewer, PR #29). TICKET-47 split the advance/skip rate limiter into a STRICT singer-skip bucket (12/60s) and a generous unplayable-watchdog bucket (40/60s), keyed on the advance request's `reason`. The allowlist already fails safe (a non-allowlisted `reason` → `skipReason=null` → `{unplayable:false}` → STRICT bucket, `route.ts` L56-61) but nothing tested it. Added two route-level tests: (1) a junk `reason` gets exactly `ADVANCE_RATE_ROOM_MAX` (12) successes then a 429 at the 13th — assertion uses the imported constant, so it can't silently drift; (2) the generous unplayable bucket stays untouched by a junk run. **Zero production-source change** — pure regression guard; prod verified already correct.
- **Selection rationale (Step 1):** boraoke is a live product where every `main` merge auto-deploys to boraoke.com → every dev item is deliver-not-merge. The two open PRs (#31 TICKET-49, #32 TICKET-50) are GATED and already delivered. The two remaining MEDs stayed unsafe unattended (pending-store MED collides with open PR #31 on `UpstashPendingStore`; TV-e2e-deflake MED is a CI-only flake unverifiable locally — both per fire #11). Picked the cleanest **safe, collision-free, verifiably-green-locally, backend-test-only** filed follow-up: FU-2 → TICKET-51. Touches no file in the open PRs (they touch pending-store / rate-limit-counter; this touches the advance rate-limit test only).
- **Gate chain (all green):** worktree `.worktrees/ticket-51` off `origin/main` → **Dev** 522/522 tests + build exit 0 (test commit `0df8b2c`, pushed) → **App Tester N/A** (backend-only, test-only, no UI) → **Cyber N/A** (test-only, no new attack surface) → **Reviewer opus D-022 APPROVE** (deterministic across 4 local runs; strict-ceiling assertion uses imported `ADVANCE_RATE_ROOM_MAX` — no hard-coded drift; verified against `lib/advance-rate-limit.ts` 12-vs-40 + `route.ts` L56-61 routing; no follow-ups). Reviewer report: `work/reports/reviewer/TICKET-51-reviewer-report.md`.
- **Decision: DELIVER, not auto-merge.** boraoke.com is a live client site with Vercel auto-deploy on `main` — an unattended fire never triggers a client-facing prod deploy, even for a zero-behavior-change test-only PR. PR **#33** left OPEN for the TL: https://github.com/paulosalvatore/boraoke/pull/33
- **Step 0.5 re-verify (load-bearing):** boraoke.com probed live (HTTP 200); `gh pr list` open before this fire = #31 + #32 (both GATED, delivered-not-merged) — board accurate, no stale premise to reconcile.
- **Open for the TL:** PR #33 (TICKET-51, test-only, gate-green), PR #32 (TICKET-50), PR #31 (TICKET-49) — all three deliver-not-merge (merge = live boraoke.com deploy). Pre-existing TL-blocked items unchanged (EN/ES OG cards debug-Chrome login, Google OAuth console origins, YouTube quota form). Orphaned merged worktrees (`.worktrees/ticket-24a/33/33b/44/46/47/48`) still safe to `worktree-cleanup` (deferred — not touching shared worktrees unattended).
- **Selection safety:** shared framework HEAD confirmed on `main`; boraoke checkout on `main`; touched no open PR's worktree/branch/files; edited only `work/status/BOARD.md` + `work/status/MANAGER-LOG.md` additively; new worktree `.worktrees/ticket-51` is this fire's own.

## 2026-07-14 — Heartbeat fire #15 (autonomous, unattended) — PROGRESSED

- **Advanced:** TICKET-53 (`[MED]` → delivered) — the pending-store poll-cost/TTL follow-up (PR #25 security LOW-3 + sonnet). Three cohesive backend-only changes to `UpstashPendingStore` in `lib/pending-store.ts`: (1) **MGET batch** — `listRoom` now fetches all indexed item records in ONE `mget` instead of a per-id `get` loop (the moderation host view polls `listRoom` every ~3s → was N+1 GETs, ~4,400 Upstash cmds/min at 20 pending); empty-ids guard prevents a zero-arg mget; chronological sort + null-omission preserved. (2) **TTL on rejected** — `reject` and `rejectAllPending` set a bounded `REJECTED_PENDING_TTL_MS` (10 min) `pexpire` on the rejected item key so orphans self-clean (was: rejected entries never expire → unbounded accumulation); never applied on `take`/approval or to a still-`pending` entry. (3) **Lazy index prune** — ids whose MGET slot is null get `lrem`'d from the index on the read path (null-slot only). Memory driver (`MemoryPendingStore`) byte-identical; public `PendingStore` interface unchanged; only two new methods (`mget`, `pexpire`) added to `PendingRedisLike`.
- **Selection rationale (Step 1):** the deliver-not-merge queue was DRAINED (interactive TM batch-merged #31–#34 on 2026-07-12; `gh pr list` open = **[]** this fire) — so the blocked-pile condition that idled fire #14 no longer holds, and one new deliver-not-merge increment is legitimate (not stacking onto an undeliverable pile). Picked the highest-value now-**unblocked** item: the `[MED]` pending-store MED was skipped by fires #11–#13 **only** because it collided with then-open PR #31 (`UpstashPendingStore`); with #31 merged it is now unclaimed, safe, backend-only, and verifiable locally. Skipped the remaining unclaimed MED (TV-e2e-deflake — a CI-only flake unverifiable locally, per #11) and TL-blocked items (EN/ES OG cards debug-Chrome login, Google OAuth console origins, YouTube quota form).
- **Gate chain (all green):** worktree `.worktrees/ticket-53` off `origin/main` → **Dev** 542/542 tests + build exit 0 (pending-store suite 27/27; worktree needed `npm install` for `next build` to resolve `next-intl`, package-lock unchanged) → **App Tester N/A** (backend-only, no UI — precedent #10–#13) → **Cyber folded into Reviewer** (internal data-store plumbing on an already-authed path, no new attack surface) → **Reviewer opus D-022 APPROVE** (independently reproduced 542/542 + build 0; verified MGET id-order + empty-guard + null-slot handling, TTL on the item key only in BOTH reject paths and never on `take`/still-pending, lazy-prune removes null ids only, memory driver untouched, and the real `@upstash/redis` client structurally satisfies the extended interface; one optional non-blocking NIT — pipeline the `set`+`pexpire` round-trips on bulk reject). Reviewer report: `work/reports/reviewer/TICKET-53-reviewer-report.md` (committed `feb89e1`).
- **Decision: DELIVER, not auto-merge.** boraoke.com is a live client site with Vercel auto-deploy on every `main` merge — a GATED carve-out; an unattended fire never triggers a client-facing prod deploy. PR **#35** left OPEN for the TL: https://github.com/paulosalvatore/boraoke/pull/35 (prod impact only applies once the Upstash pending driver is provisioned — prod currently runs the in-memory driver).
- **Step 0.5 re-verify (load-bearing):** boraoke.com probed live (**HTTP 200** from this container — note the 07-12 interactive TM container was 403-blocked outbound and could not verify; this fire could). Board already accurate: the interactive TM drained the pile on 07-12 (header + Needs-user RESOLVED note) and the F110 `*.jsonl merge=union` driver is already ported into boraoke (`faf4020`) — **no stale premise to reconcile.** Also corrected the Tickets-table drift where #49/#50/#51/#52 still read OPEN (all merged 07-12) → MERGED, additively.
- **Open for the TL:** PR #35 (TICKET-53, gate-green, deliver-not-merge = live deploy on merge). Pre-existing TL-blocked items unchanged (EN/ES OG cards debug-Chrome login, Google OAuth console origins, YouTube quota form). Orphaned merged worktrees (`.worktrees/ticket-24a/33/33b/44/46/47/48/49/50/51/52`) remain safe to `worktree-cleanup` (deferred — not touching shared worktrees unattended).
- **Selection safety:** boraoke checkout on `main` (local ff'd to `origin/main`); touched no other worktree/branch/PR; edited only `work/status/BOARD.md` + `work/status/MANAGER-LOG.md` additively; new worktree `.worktrees/ticket-53` is this fire's own.
- **Outcome: progressed.**

## 2026-07-21 — Heartbeat fire #22 (autonomous, unattended) — PROGRESSED

- **Step 0.5 repair (stranded #21 state):** heartbeat #21 (earlier today) reconciled the board but **died before committing** — BOARD.md was dirty on `main`, no #21 MANAGER-LOG entry existed, and local `main` was 3 commits behind `origin/main` (the TL's 07-20 merges of PRs #36/#37/#38). Repaired: stashed the edits, ff'd `main` to `1f24e33`, re-applied #21's reconciliation onto the new base (one conflict — upstream's stale "TICKET-31 IN REVIEW" row vs #21's correct MERGED rows; kept #21's), committed this fire.
- **Housekeeping (board-flagged safe):** removed `.worktrees/anon-identity-registry` + `.worktrees/31-admin-analytics` after verifying PR #37/#38 state=MERGED with branch tips == PR headRefOids; only disposable event-log noise inside; local branches deleted, remote branches kept.
- **Advanced:** **TICKET-55 — Upstash-backed YouTube search cache** (P0 operational debt, "biggest quota lever", PR #8 opus rec; the ONLY P0/P1 dev item needing no TL decision). Scope = cache half only (FU-2b rate-buckets half stays deferred). New `lib/search-cache.ts` (driver pattern mirrors `rate-limit-counter.ts`: STORE_DRIVER → auto-Upstash → memory; two-tier memory-L1→Redis with L1 warming; `sc:` namespace; shape-guard on payloads; fail-open on every Redis call), `/api/search` reads cache before the YouTube call and writes only after success (12h TTL non-empty, 10min empty, errors never cached), `cacheKey` whitespace-collapse; **rate limiter byte-identical** and still charges BEFORE the cache read (no bypass). 14 new tests.
- **Selection rationale:** pile drained (open PRs = [] pre-fire), TL merged 3 PRs yesterday (active merge throughput; grow-vs-hold implicitly "grow"), no parallel driver in ~19h (last activity 07-20 22:16Z) → one deliver-not-merge increment legitimate per #15/#17 precedent. Skipped: TICKET-27 (blocked on bot-vendor decision 3), all TL-decision items (1,4-8), Upstash pending-driver provisioning (prod env change = GATED ops), TV-e2e deflake (CI-only, unverifiable locally), frontend nits (need visual gate, LOW).
- **Gate chain (all green):** worktree `.worktrees/ticket-55` off `origin/main` @ `1f24e33` → **Dev** 608/608 tests (43 suites) + build exit 0, commit `f48c62b`, package-lock unchanged, no new deps → **App Tester N/A** (backend-only, response shapes unchanged — precedent #10–#17) → **Cyber folded into Reviewer, PASS** (no cache-key injection/poisoning — fixed server regionCode + `sc:` namespace; empty-poisoning capped at 10min; no secrets logged; no new addressable surface) → **Reviewer opus D-022 APPROVE** (independently reproduced 608/608 + build 0; diff = exactly the 7 claimed files; limiter-vs-cache ordering judged SAFE; real @upstash/redis 1.38.0 interface verified). Review report committed on-branch: `work/reports/review/TICKET-55-review.md` (`f28be4f`).
- **Decision: DELIVER, not auto-merge.** boraoke is fully GATED — every `main` merge auto-deploys live boraoke.com; an unattended fire never triggers a client-facing prod deploy. **PR #39 left OPEN for the TL:** https://github.com/paulosalvatore/boraoke/pull/39 (with Upstash already provisioned in prod, the cache is effective immediately on deploy).
- **Open for the TL:** merge PR #39 (= live deploy, your call) + the remaining growth-arc decisions (bot vendor blocks TICKET-27 next; YT quota, payments, paid feature, venues, i18n, cadence) + provision the Upstash pending driver + ADVANCE_AUTH enforce flip.
- **Selection safety:** framework HEAD on `main`; boraoke `main` == `origin/main`; touched no other driver's worktree/branch/PR; `.worktrees/ticket-55` is this fire's own; status docs edited additively.
- **Outcome: progressed.**

## 2026-07-21 — Heartbeat fire #23 (autonomous, unattended) — BLOCKED-PILE

- **Step 0.5 re-verify (all GREEN, no stale premise):** boraoke.com probed live (HTTP 200); `gh pr list --state open` = **only PR #39** (TICKET-55 Upstash search cache), `mergeable=MERGEABLE`, delivered gate-green by heartbeat #22 earlier today (last update 11:52Z). No live agent-heartbeats, no commits in the prior 90 min — no parallel driver. Board accurate as reconciled by #21/#22.
- **No new dev PR this fire:** boraoke is fully-GATED (every `main` merge auto-deploys live boraoke.com) so an unattended fire never merges; PR #39 already sits open awaiting the TL. Every remaining backlog item is either blocked on the open growth-arc TL decisions (bot vendor → TICKET-27; YT quota, payments, paid feature, venues, i18n, cadence), GATED prod ops (provision Upstash pending driver, ADVANCE_AUTH enforce flip), CI-only-unverifiable locally (TV-e2e deflake), a real deferred design change (FU-2b dual-bucket search limiter; GET patronUuid projection entangled with own-row highlighting), or LOW/NIT frontend work needing a visual gate — minting any of these would only grow the undeliverable deliver-not-merge pile (the token-burn pattern fires #14/#16/#19/#20 flagged; constraint = human-merge throughput, not dev throughput). Backlog fully groomed (PO sweep 07-15, reconciled #21/#22) → no genuine grooming increment either.
- **Decision:** no merge, no new PR, no new inventory. **Ball with the TL: merge PR #39 (= live deploy, your call) + answer the growth-arc decisions.** https://github.com/paulosalvatore/boraoke/pull/39
- **Selection safety:** framework HEAD on `main`; boraoke checkout on `main` == `origin/main`; touched no worktree/branch/PR (`.worktrees/ticket-55` belongs to open PR #39 — kept); edited only `work/status/BOARD.md` + `work/status/MANAGER-LOG.md` additively.
- **Outcome: blocked-pile.**

## 2026-07-22 — Heartbeat fire #24 (autonomous, unattended) — BLOCKED-PILE

- **Step 0.5 re-verify (all GREEN, no stale premise):** boraoke.com probed live (HTTP 200); `gh pr list --state open` = **only PR #39** (TICKET-55 Upstash search cache), checks build-and-test + Vercel = SUCCESS. GitHub reported `mergeable=UNKNOWN` (transient recompute, F110-class caution) → re-verified locally: `git merge-tree --write-tree origin/main origin/ticket/55-upstash-search-cache` **exit 0 = CLEAN** — #39 merges cleanly; not conflicted. No live boraoke agent-heartbeats, no commits in the prior 90 min — no parallel driver. Board accurate as left by #23.
- **No new dev PR this fire:** constraint set identical to #23. Boraoke is fully-GATED (every `main` merge auto-deploys live boraoke.com) so an unattended fire never merges; PR #39 already sits open awaiting the TL. Every remaining item is blocked on the growth-arc TL decisions (bot vendor → TICKET-27; YT quota, payments, paid feature, venues, i18n, cadence), GATED prod ops (Upstash pending driver provisioning, ADVANCE_AUTH enforce flip), CI-only-unverifiable locally (TV-e2e deflake), a real deferred design change (FU-2b, GET patronUuid projection), or LOW/NIT pile-growers. Backlog groomed (PO sweep 07-15, reconciled #21/#22) → no genuine grooming increment.
- **Decision:** no merge, no new PR, no new inventory. **Ball with the TL: merge PR #39 (= live deploy, your call) + answer the growth-arc decisions.** https://github.com/paulosalvatore/boraoke/pull/39
- **Selection safety:** framework HEAD on `main`; boraoke checkout on `main` == `origin/main`; touched no worktree/branch/PR (`.worktrees/ticket-55` belongs to open PR #39 — kept); edited only `work/status/BOARD.md` + `work/status/MANAGER-LOG.md` additively.
- **Outcome: blocked-pile.**

## 2026-07-23 — Heartbeat fire #25 (autonomous, unattended) — BLOCKED-PILE

- **Step 0.5 re-verify (all GREEN, no stale premise):** boraoke.com probed live (HTTP 200); `gh pr list --state open` = **only PR #39** (TICKET-55 Upstash search cache), **mergeable=MERGEABLE / mergeStateStatus=CLEAN**, checks build-and-test + Vercel + Preview Comments all SUCCESS. No live boraoke agent-heartbeats, no commits since heartbeat #24 (2026-07-22) — no parallel driver. Board accurate as left by #24.
- **No new dev PR this fire:** constraint set identical to #23/#24. Boraoke is fully-GATED (every `main` merge auto-deploys live boraoke.com) so an unattended fire never merges; PR #39 already sits open awaiting the TL. Every remaining item is blocked on the growth-arc TL decisions (bot vendor → TICKET-27; YT quota, payments, paid feature, venues, i18n, cadence), GATED prod ops (Upstash pending driver provisioning, ADVANCE_AUTH enforce flip), CI-only-unverifiable locally (TV-e2e deflake), a real deferred design change (FU-2b, GET patronUuid projection), or LOW/NIT pile-growers. Backlog groomed (PO sweep 07-15, reconciled #21/#22) → no genuine grooming increment.
- **Decision:** no merge, no new PR, no new inventory. **Ball with the TL: merge PR #39 (= live deploy, your call) + answer the growth-arc decisions.** https://github.com/paulosalvatore/boraoke/pull/39
- **Selection safety:** framework HEAD on `main`; boraoke checkout on `main` == `origin/main`; touched no worktree/branch/PR (`.worktrees/ticket-55` belongs to open PR #39 — kept); edited only `work/status/BOARD.md` + `work/status/MANAGER-LOG.md` additively.
- **Outcome: blocked-pile.**

## 2026-07-24 — Heartbeat fire #26 (autonomous, unattended) — 🟢 PROGRESSED (stale-premise reconciliation, no new PR)

- **Step 0.5 re-verify — PR state unchanged, but ONE premise was STALE.** boraoke.com probed live (**HTTP 200**); `gh pr list --state open` = **only PR #39** (TICKET-55 Upstash search cache), **mergeable=MERGEABLE / mergeStateStatus=CLEAN**, build-and-test + Vercel + Preview Comments all SUCCESS — identical to #23/#24/#25, ball still with the TL. No parallel driver (no boraoke agent-heartbeats; no commits since heartbeat #25 on 07-23).
- **The stale premise (this fire's increment): there are TWO local clones of this repo, and the board pointed at the dead one.** The framework board still said "local checkout dir still `~/Documents/GitHub/cantai` until the solo-window rename". Reality: the rename happened and a second clone exists. `~/Documents/GitHub/boraoke` is **LIVE** (HEAD == `origin/main` `deca871`, `.worktrees/ticket-55` = open PR #39, 608 MB) — heartbeats #18–#25 all ran there. `~/Documents/GitHub/cantai` is **ABANDONED**: last commit 2026-07-16 (heartbeat #17), main **16 commits behind** origin, **13 orphaned worktrees** for long-merged PRs (24a, 33, 33b, 44, 46–54), **7.0 GB** on disk. I nearly resumed this fire from the dead clone — a real trap for any future driver.
- **Safety verification before recommending deletion (nothing was deleted or moved).** `cantai` holds **zero unique git work**: `git stash list` empty; every local branch is **0-ahead** of its origin counterpart (`main` 16 behind/0 ahead, `ticket/44` 28/0, `ticket/49` 13/0, `ticket/50` 8/0, `ticket/51` 8/0, `ticket/52` 5/0); all 13 worktrees clean (`dirty=0`); only untracked files are gitignored heartbeat JSONs. **However** — `git status --ignored` surfaced **`.env` (4128 B, 2026-07-07) and `.env.local` (1748 B)** present ONLY in `cantai`; the live clone has just `.env.example`. So the dead clone is the sole local holder of the env secrets, and the live clone currently cannot run locally.
- **Decision: report, do NOT act.** Secrets are an explicit GATED carve-out (prod DB / PII / deploys / DNS / money / **secrets**) — an unattended fire never relocates them, and deleting a 7 GB clone whose fate is a TL call is not heartbeat work. Left both clones, all 13 worktrees, and every branch untouched. Recorded the correction additively in `BOARD.md` (header + a new Needs-user item) with the exact two-step TL action: (1) review + copy `.env`/`.env.local` into the live clone — they predate the rename, so `NEXTAUTH_URL` and other `cantai`-era values likely need updating to `boraoke.com`; (2) then `rm -rf ~/Documents/GitHub/cantai`, reclaiming ~6.4 GB.
- **Class-level prevention (filed, not fixed here):** "product rename leaves a stale duplicate clone that later drivers can silently resume from" is a framework-class hazard, not a boraoke bug — filed as a `work/self-improvement/inbox/` note for the framework TM per CLAUDE.md §6 rather than fixed from this product context.
- **No new dev PR:** constraint set otherwise identical to #23/#24/#25 — fully-GATED product (every `main` merge auto-deploys live boraoke.com), PR #39 already open awaiting the TL, remaining items blocked on growth-arc TL decisions (bot vendor → TICKET-27; YT quota, payments, paid feature, venues, i18n, cadence), GATED prod ops (Upstash pending driver, ADVANCE_AUTH flip), CI-only-unverifiable (TV-e2e deflake), real deferred design changes (FU-2b, GET patronUuid projection), or LOW/NIT pile-growers.
- **Ball with the TL:** merge PR #39 (= live deploy, your call) · do the two-step checkout cleanup above · answer the growth-arc decisions. https://github.com/paulosalvatore/boraoke/pull/39
- **Selection safety:** framework HEAD on `main`; live boraoke checkout on `main` == `origin/main`; touched no worktree/branch/PR (`.worktrees/ticket-55` belongs to open PR #39 — kept); edited only `work/status/BOARD.md` + `work/status/MANAGER-LOG.md` additively.
- **Outcome: progressed.**

## 2026-07-25 — Heartbeat fire #27 (autonomous, unattended) — 🟢 PROGRESSED (new gate-green PR #40 + a stale-claim reconciliation)

- **Step 0 cold-resume clean.** Framework HEAD on `main`. Ran the F193 behind-origin guard in the **canonical** boraoke clone (`~/Documents/GitHub/boraoke`, deliberately NOT the abandoned `cantai` duplicate that #26 exposed): `origin/HEAD` resolved to `origin/main`, fetch OK, **behind-count 0** → tree verified current, board readable as authoritative.
- **Step 0.5 re-verified every load-bearing claim rather than trusting the board.** boraoke.com **HTTP 200**; `gh api .../commits/main/status` → **success** (probed the commit-status rollup, not just Actions, per the quiz-ilp lesson); **PR #39 mergeable=MERGEABLE / mergeStateStatus=CLEAN**, all checks SUCCESS — unchanged since #23–#26 and genuinely still awaiting the TL. No stale dismissal was inherited or relied on.
- **Second Step 0.5 finding — a stale board claim, reconciled.** The 2026-07-15 roadmap-reconciliation line "F110 `merge=union` never ported" **contradicted** board line 38 ("boraoke now carries the `merge=union` driver, `faf4020`"). Read `.gitattributes` directly: it **does** carry `*.jsonl merge=union` with the full TICKET-F110 rationale. Line 38 was right, the sweep line wrong. Corrected additively in place, together with two adjacent stale items in the same paragraph (the Upstash search cache **has** been built — it is PR #39; the "stale cantai naming" debt is now partly done). Also corrected the board's H1 from `# cantai — Board` to `# boraoke — Board`.
- **Step 1 selection — deferential, non-competing.** `.worktrees/ticket-55` + open PR #39 = another driver's in-flight work → treated as **CLAIMED and skipped**. No agent-heartbeats, no other worktrees, no recent foreign commits. Picked a genuinely unclaimed, lower-in-the-list item that needs **no TL decision**: the filed follow-up **TICKET-49 F-1** → **TICKET-56**, backend-only (App Tester N/A), mirroring two already-merged in-repo precedents (`MERGE_SCRIPT`, `REGISTER_FAILURE_SCRIPT`), fully unit-testable against the existing `FakeRedis`, and verified disjoint from PR #39's file set.
- **Step 2 — full gate chain, in a fresh worktree off `origin/main`.** `.worktrees/ticket-56` @ `9470051`. Dev made `UpstashPendingStore.rejectAllPending` atomic via a single Lua `EVAL`, closing the lost-update race. **Dev: jest 599/599 across 42 suites (branch baseline 594, +5 new tests), `next build` exit 0.** **Reviewer (opus, D-022): APPROVE-WITH-FOLLOWUPS**, Cyber **folded PASS**. The reviewer reproduced both gates itself and **empirically falsified the new tests against old code** (new tests vs old impl → 1 failed at the eval round-trip assertion; a warn-stripped scratch build → 1 failed at `expect(warn).toHaveBeenCalled()`), proving they are discriminating rather than vacuous. Cyber reasoning was concrete: no Lua-injection primitive (static script constant; `roomId` only ever reaches `KEYS[1]`/`ARGV[1]`, never the script text) and no cross-room escape (`ROOM_ID_RE = /^[a-z0-9-]{1,64}$/` forbids the `:` separator); writes bounded to `room:<id>:pending:item:*`, no `DEL`.
- **Folded four reviewer follow-ups in before delivering** (FU-2 silent-degradation warn, FU-1 comment-half, FU-3 Redis-Cluster caveat, FU-4 O(N)-blocking cost note) and had the reviewer **CONFIRM** the delta; deferred three to the board (FU-1b behavioral half, FU-5, FU-7). All commits went through the sanctioned `commit` skill with explicit file lists; never bare git, never force-push.
- **Step 3 — DELIVERED, NOT MERGED. PR #40 is fully gate-green** (`build-and-test` SUCCESS, mergeable=MERGEABLE/CLEAN) **but merging lands on `main`, which auto-deploys live boraoke.com** — the product's GATED carve-out. An unattended fire never triggers a live client deploy. https://github.com/paulosalvatore/boraoke/pull/40
- **Disclosed record defect (not hidden, not papered over):** the commit body of `87709d5` misdescribes the mechanism as "add to rejected set" — there is no rejected set; the script flips `status` in place on each item key. Code, docblocks, PR body and reviewer report are all correct. Not amended because the sanctioned mechanism never force-pushes; the correction is recorded in the PR body, on the board, and in the reviewer report.
- **Ball with the TL:** merge **PR #39** and **PR #40** (each = a live deploy, your call) · the two-step `cantai` checkout cleanup from #26 (still holds the only local `.env`/`.env.local`) · the growth-arc decisions. The open-PR pile is now **two** — the structural pattern flagged since #14: a productive fire can only grow this queue, since nothing here is auto-mergeable.
- **Selection safety:** touched no other driver's worktree, branch or PR; `.worktrees/ticket-55` left untouched; all status-doc edits additive.
- **Outcome: progressed.**

## 2026-07-25 — Heartbeat fire #28 (autonomous, unattended) — 🟢 PROGRESSED (closed out an explicitly-unverified item, found a live defect in it, filed TICKET-57; no new PR)

- **Step 0 cold-resume clean.** Ran the F193 behind-origin guard in the canonical clone `~/Documents/GitHub/boraoke` (deliberately NOT the abandoned `cantai` duplicate that #26 exposed): `origin/HEAD` → `origin/main`, fetch OK, HEAD on `main`, **behind-count 0** → tree verified current, board readable as authoritative.
- **Step 0.5 re-verified every load-bearing claim rather than trusting the board.** boraoke.com **HTTP 200**; `gh api .../commits/main/status` → **success** (commit-status rollup, not just Actions, per the quiz-ilp lesson; `check-runs` empty, Vercel posts a commit status). **PR #39 and PR #40 both OPEN, both `MERGEABLE`/`CLEAN`**, all three checks SUCCESS on each. One near-miss handled: `gh pr list` reported #39 `mergeable=UNKNOWN`, which the board's own F110 guidance says never to read as un-mergeable — `gh pr view 39` resolved it to MERGEABLE/CLEAN (transient recompute), so no local `merge-tree` re-verify was needed. **No stale dismissal was inherited or relied on.**
- **The fire's increment — closing out an item a prior fire explicitly left open.** Heartbeat #27 corrected the roadmap sweep's naming line but stated plainly that `CLAUDE.md` and the roadmap doc "were **not** checked and may still carry it." This fire checked them, and the unchecked gap was hiding a **live defect, not a cosmetic one**: this repo's `CLAUDE.md` line 15 instructs the interactive TM to archive via `scripts/archive-prompt.sh --product cantai`. **`cantai` is not a registered slug** — the framework's single-source registry `scripts/products.txt` carries only `boraoke` (read directly). Per `archive-prompt.sh` L141-145 an unknown slug is **coerced to `unclassified` with a warning and never rejected** (TICKET-F36 deliberately keeps F31 normalize-not-hard-fail so a typo can never block the mandatory archiving duty) — so the failure is silent and every prompt archived from a boraoke session per that repo's own rules drops out of the per-product index.
- **Second half of the finding — a hazard, recorded so a future driver does not "fix" it.** `grep -ril cantai` also hits **product source**, and those hits are **deliberate and must NOT be renamed**: the live localStorage keys `cantai_patron_uuid` / `cantai_nickname` / `cantai_mode` / `cantai_room` / `cantai_last_room` / `cantai:<room>:nick` / `cantai:<room>:table`, the legacy `cantai-snowy.vercel.app` host redirect in `next.config.ts:19`, and `__tests__/metadata.test.ts:16` — which is a **negative** assertion (`not.toMatch(/cantai/i)`) and correct as written. Each already carries its own "STORAGE-KEY NOTE (TICKET-33 rebrand): DELIBERATELY kept" comment. **Renaming the keys would drop every live patron's identity, nickname and room memory.** A bulk `sed` over this repo is a live regression dressed as housekeeping, which is precisely why this went onto the board as an explicit hazard rather than into a PR.
- **Filed as TICKET-57** (Follow-ups + Tickets table), bundling (a) the real fix, (b) the per-file cosmetic doc decisions, and (c) the do-not-touch hazard list. This converts the sweep's long-floating unscoped "stale cantai naming" debt line into one bounded, safe housekeeping ticket.
- **Also corrected a small table drift:** TICKET-56 was filed by #27 under Follow-ups but never added to the Tickets table — added (DELIVERED, PR #40 OPEN).
- **Step 1 selection — deferential, non-competing.** `.worktrees/ticket-55` (PR #39) and `.worktrees/ticket-56` (PR #40) are other drivers' in-flight work → treated as **CLAIMED and skipped**. No agent-heartbeats, no other worktrees, no commits since #27's own at 09:10 today. Every remaining dev item is blocked on the growth-arc TL decisions (bot vendor → TICKET-27; YT quota, payments, paid feature, venues, i18n, cadence), GATED prod ops (Upstash pending driver provisioning, ADVANCE_AUTH enforce flip, the `cantai` clone's `.env` relocation — secrets), CI-only-unverifiable locally (TV-e2e deflake), a real deferred design/judgment call (FU-2b dual-bucket limiter; TICKET-56 FU-1b, deferred by #27 precisely because it is a TL judgment), or LOW/NIT pile-growers.
- **Decision: NO third PR — deliberately.** The pile is already **two** gate-green undeliverable PRs and #27 delivered one ~3h ago. boraoke can never auto-merge unattended (every `main` merge auto-deploys the live site), so the binding constraint is human-merge throughput, not dev throughput — the structural pattern flagged since fire #14. Minting a docs-only third PR would grow the queue for no throughput gain. The skill's sanctioned alternative applies exactly here: with no safe actionable *dev* task, groom into ONE concrete follow-up ticket — which is a genuine one, not invented busywork.
- **Ball with the TL:** merge **PR #39** and **PR #40** (each = a live deploy, your call) · the two-step `cantai` checkout cleanup from #26 (still holds the only local `.env`/`.env.local`) · the growth-arc decisions. TICKET-57 is ready to pick up whenever the pile drains.
- **Selection safety:** framework HEAD on `main`; boraoke checkout on `main` == `origin/main`; **created no worktree and no branch**; touched no other driver's worktree, branch or PR; edited only `work/status/BOARD.md` + `work/status/MANAGER-LOG.md`, additively.
- **Outcome: progressed.**

## 2026-07-26 — Heartbeat fire #29 (autonomous, unattended) — 🟡 BLOCKED-PILE (pile confirmed deliverable + non-stale; no new PR)

- **Step 0 cold-resume clean.** Ran the F193 behind-origin guard in the canonical clone `~/Documents/GitHub/boraoke` (deliberately NOT the abandoned `cantai` duplicate that #26 exposed — verified `git remote -v` → `git@github.com:paulosalvatore/boraoke.git`): `origin/HEAD` → `origin/main`, fetch OK, HEAD on `main`, **behind-count 0** → tree verified current, board readable as authoritative.
- **Step 0.5 re-verified every load-bearing claim rather than trusting the board.** boraoke.com **HTTP 200**; `gh api .../commits/main/status` → **success**, and I checked the `target_url` account rather than just the state (`paulosalvatores-projects/boraoke` — the correct project, per the quiz-ilp lesson that a same-named check can silently change scope). **PR #39 and PR #40 both OPEN, both `MERGEABLE`/`CLEAN`**, all three checks SUCCESS on each (`build-and-test`, `Vercel`, `Vercel Preview Comments`). No inherited dismissal was relied on. No parallel driver: no commits in the last 6h, no agent-heartbeats, only the two worktrees belonging to the two open PRs.
- **The fire's increment — a staleness check no prior fire had run on the pile.** Fires #23–#28 each re-confirmed the PRs were *green*, but never asked whether that green still *means* anything. PR #39's `build-and-test` last ran **2026-07-21 — five days and 14 `main` commits ago**; a green check against a base that has since moved is a stale artifact, not evidence. Probed it directly: `git log <merge-base>..origin/main -- app lib components packages scripts __tests__ e2e package.json next.config.ts` returns **EMPTY for both PRs**. Every one of the 14 intervening commits for #39 (and 4 for #40) is a status-doc or event-log commit — **zero product code has landed on `main` since either branch point**. So both PRs' full gate results remain valid as-is: **no rebase, no re-run, no re-gate needed.**
- **Second half — the two PRs are file-disjoint, so batch-merging is conflict-free.** #39 touches `lib/search-cache.ts`, `lib/youtube-search.ts`, `app/api/search/route.ts` and their tests; #40 touches `lib/pending-store.ts` and its test. No overlap. Combined with the staleness result, this upgrades the standing "merge them when you're ready" into a concrete, de-risked instruction: **the TL can merge both back-to-back, in either order, in one sitting, with zero rebase work.** That directly attacks the actual bottleneck — human-merge throughput — which is the most useful thing an unattended fire can do for this product.
- **Step 1 selection — deferential, non-competing.** `.worktrees/ticket-55` (PR #39) and `.worktrees/ticket-56` (PR #40) are other drivers' in-flight work → treated as **CLAIMED and skipped**. Every remaining dev item is blocked on the growth-arc TL decisions (bot vendor → TICKET-27; YT quota, payments, paid feature, venues, i18n, cadence), GATED prod ops (Upstash pending driver provisioning, ADVANCE_AUTH enforce flip, the `cantai` clone's `.env` relocation — secrets), CI-only-unverifiable locally (TV-e2e deflake), a real deferred design/judgment call (FU-2b dual-bucket limiter; TICKET-56 FU-1b), or LOW/NIT pile-growers.
- **Decision: NO third PR — reaffirming #28's call, on unchanged grounds.** The pile is already two gate-green undeliverable PRs. boraoke can never auto-merge unattended (every `main` merge auto-deploys the live client site), so the binding constraint is human-merge throughput, not dev throughput — the structural pattern flagged since fire #14. Minting a third PR (TICKET-57's docs-only `--product cantai` → `boraoke` fix is the obvious candidate) would grow the queue for zero throughput gain. TICKET-57 stays groomed and ready to pick up the moment the pile drains. Nothing was invented to look productive.
- **Ball with the TL:** batch-merge **PR #39** and **PR #40** (now verified as one cheap sitting, either order, no rebase) · the two-step `cantai` checkout cleanup from #26 (still holds the only local `.env`/`.env.local`) · the growth-arc decisions.
- **Selection safety:** boraoke checkout on `main` == `origin/main`; **created no worktree and no branch**; touched no other driver's worktree, branch or PR; edited only `work/status/BOARD.md` + `work/status/MANAGER-LOG.md`, additively.
- **Outcome: blocked-pile.**

## 2026-07-28 — Heartbeat fire #32 (autonomous, unattended) — 🟢 PROGRESSED (new gate-green PR #42 + a class-level safety defect found and contained)

- **Step 0 cold-resume clean.** Ran the F193 behind-origin guard in the canonical clone `~/Documents/GitHub/boraoke` (verified `git remote -v` → `git@github.com:paulosalvatore/boraoke.git`, deliberately NOT the abandoned `cantai` duplicate): `origin/HEAD` → `origin/main`, fetch OK, HEAD on `main`, **behind-count 0** → tree verified current, board readable as authoritative.
- **Step 0.5 re-verified every load-bearing claim rather than trusting the board.** boraoke.com **HTTP 200**; `gh api .../commits/main/status` → **success**, `target_url` on `paulosalvatores-projects/boraoke` (correct account re-derived, not inherited — per the quiz-ilp lesson). **PRs #39, #40, #41 all still OPEN.** #39 and #40 both reported `mergeable=UNKNOWN`; per the F110 rule I did not read that as un-mergeable but re-verified locally — `git merge-tree --write-tree origin/main <branch>` **exit 0 for both = clean**, a transient recompute. No inherited dismissal was relied on. No parallel driver: zero commits in 6h, no agent-heartbeats, only the three worktrees belonging to the three open PRs.
- **Step 1 selection — deferential, non-competing.** `.worktrees/ticket-55` (#39), `ticket-56` (#40), `ticket-57` (#41) treated as **CLAIMED and skipped**. Picked **TICKET-58** — unclaimed, TL-decision-free, docs-only, file-disjoint from all three open PRs. **Verified its premise before building on it** (the whole point of Step 0.5): the stale clone still exists and is **28 commits behind `origin/main`** — the board's last figure was 16, so the hazard had grown, not evaporated.
- **Why grow the pile to four, when #28/#29/#30 all declined to grow it past two.** Deliberate and narrow: TICKET-58 is not a LOW-value feature, it is an **active defect in the fleet's own tooling** — every agent following `run-app` silently ran a two-weeks-stale checkout and produced results that *looked* valid. That degrades the reliability of every subsequent fire, including this one. Marginal merge cost is ~zero: #29 established the batch-merge is one cheap sitting, and I re-verified this branch is `merge-tree`-clean and file-disjoint against all three open PRs, so it adds no rebase work and no conflict risk.
- **Step 2 — full gate chain in a fresh worktree off `origin/main`.** `.worktrees/ticket-58` @ `3382245`. Dev repointed both paths at the canonical clone and generalised the hardcoded `.worktrees/ticket-1` section (TICKET-1 is long merged) to the D-033 `<slug>` placeholder, so it cannot rot the same way again. **1 file, +5/-3.** App Tester **N/A** (docs-only, no UI), Cyber **N/A** (no auth/data/input surface). Reviewer **opus D-022 APPROVE-WITH-FOLLOWUPS**, all 7 claims independently re-derived — including the 28-behind count, and the safety invariant: `cantai` hazard identifiers **60 lines / 64 occurrences before, byte-identical after** across the same 25 files, all 29 hazard lines intact, repo-wide occurrences 791 → 789 (exactly the two removed path strings) — **no bulk sed**. Reviewer polled CI to pass rather than approving on pending.
- **THE LARGER FINDING — a class-level safety defect, found because the Dev stopped instead of working around.** `commit-and-push.sh` failed with `"offline or non-fast-forward"`. **It was not offline** (`git ls-remote --exit-code origin HEAD` succeeded). The worktree branch's upstream was `branch.<ticket-branch>.merge = refs/heads/main`, so the sanctioned script was pushing a ticket branch's commits **directly onto `origin/main` — bypassing the PR and the entire gate chain, and auto-deploying live boraoke.com.** Branch protection rejected it; **that rejection was the safety net working, not a glitch.**
  - **Root cause (reviewer-confirmed):** `git worktree add -b <branch> origin/main` makes git auto-track the start-point. That is the D-033 pattern, so **every heartbeat-created worktree carries this landmine.**
  - **It retroactively explains heartbeat #31's disclosed hand-push on PR #41** — same root cause, misdiagnosed as "offline" because that is what the script's message says, and therefore logged as a one-off process lapse rather than the systemic defect it is. Audit: `ticket/55` ✅, `ticket/56` ✅ (both self-repaired by their `push -u`), **`ticket/57` (PR #41) 🔴 still wrong today** (#31's `git push origin <branch>` creates the remote ref but does not repair the config), `ticket/58` ✅ repaired.
  - **Contained correctly, with no rewrite:** delegated to `git-commit-writer` to run `git push -u origin ticket/58-...`, creating the branch's own remote ref and fixing the upstream. **`origin/main` verified unchanged at `3382245` before and after**, `git merge-base --is-ancestor <head> origin/main` → **false** (nothing leaked), no force-push, no amend. Re-derived independently by the reviewer as the review's most important check. **I did NOT repair `ticket/57`** — it belongs to an open PR's worktree and an unattended fire does not touch another driver's work; flagged on the board with the exact one-line fix.
  - **Class-level prevention filed, not fixed from here** (CLAUDE.md §6): `work/self-improvement/inbox/2026-07-28-worktree-upstream-points-at-main.md`, proposing (1) `--no-track` at worktree creation, (2) a refuse-to-push guard in `commit-and-push.sh` when HEAD is a non-default branch whose upstream *is* the default branch, (3) an end to the mislabelled "offline or non-fast-forward" message that caused the #31 misdiagnosis, (4) the `ticket/57` one-off repair, (5) a cheap fleet-wide read-only sweep.
- **Step 3 — DELIVERED, NOT MERGED. PR #42 is fully gate-green** (`MERGEABLE`/`CLEAN`, `build-and-test` + `Vercel` + `Vercel Preview Comments` all SUCCESS) **but merging lands on `main`, which auto-deploys live boraoke.com** — the product's GATED carve-out. An unattended fire never triggers a live client deploy, even a no-op rebuild. https://github.com/paulosalvatore/boraoke/pull/42
- **Honest gaps, recorded not hidden:** `npm test` was **skipped, not faked** (no `node_modules` in the fresh worktree; docs-only change with zero runtime consumers) — reviewer judged this acceptable and CI covered the branch. No `work/tickets/TICKET-58-*.md` was filed, unlike #39/#40 — reviewer flagged it as D-002 drift. The reviewer's own `work/reports/review/` artifact was not committed (my read-only constraint); the PR comment is the record.
- **Ball with the TL:** batch-merge **#39, #40, #41, #42** (all four verified file-disjoint and `merge-tree`-clean — one sitting, any order, zero rebase) · **repair `ticket/57`'s upstream before committing from that worktree again** · the two-step `cantai` clone cleanup, which is now also the **root-cause fix** for TICKET-58 (that clone holds 15 worktrees each with its own wrong `SKILL.md` copy) · the growth-arc decisions · the ADVANCE_AUTH enforce flip.
- **Selection safety:** boraoke checkout on `main` == `origin/main`; worked only in `.worktrees/ticket-58` (this fire's own, off `origin/main`); touched no other driver's worktree, branch or PR; no product source, no env var, no secret, no deploy; status-doc edits additive only.
- **Outcome: progressed.**

## 2026-07-29 — Heartbeat fire #34 (autonomous, unattended) — 🟢 PROGRESSED (recovered #33's stranded work; refuted a safety claim #32 left on the board; no new PR)

- **Step 0 cold-resume clean.** F193 behind-origin guard in the canonical clone `~/Documents/GitHub/boraoke` (`git remote -v` → `git@github.com:paulosalvatore/boraoke.git`, deliberately NOT the abandoned `cantai` duplicate): `origin/HEAD` → `origin/main`, fetch OK, HEAD on `main`, **behind-count 0** → tree verified current, board readable as authoritative. Framework HEAD on `main`.
- **Step 0.5 re-verified every load-bearing claim rather than inheriting it.** boraoke.com **HTTP 200**; `gh api .../commits/main/status` → **success**, `target_url` on `paulosalvatores-projects/boraoke` (correct account re-derived, per the quiz-ilp lesson). **PRs #39, #40, #41, #42 all still OPEN**, all four `MERGEABLE`/`CLEAN`. Re-ran the #29 staleness probe across all four: **zero product-code commits on `main` since every branch point**, so all four sets of green checks are still meaningful; `git merge-tree --write-tree origin/main <branch>` **exit 0 (clean) for all four**; file sets **fully disjoint** (F124 gives each branch its own event-log file). Confirmed **nothing has leaked to `main`** — `git merge-base --is-ancestor` false for all four, `origin/main` at `436ee30`. Net: **the batch-merge remains one cheap sitting, any order, zero rebase.**
- **Parallel-driver check:** no commits in 6h, no live agent-heartbeats, the only running heartbeat process is this fire itself (PID verified). The four `.worktrees/ticket-5*` belong to the four open PRs → **CLAIMED and skipped**.
- **Finding 1 — heartbeat #33's work was STRANDED, and this fire recovered it.** Fire #33 ran at 10:52 today and the runner recorded `progressed`, but its Step 4 never completed: `work/tickets/TICKET-59-branch-protection-on-main.md` was left **untracked and uncommitted**, and neither `BOARD.md` nor this log was updated. The board therefore still ended at #32, and the ticket was one `git clean` from being lost — the same failure mode #22 found for #21. Committed this fire, with its claims verified rather than merely rescued.
- **Finding 2 — #32's safety attribution is REFUTED, which makes the hazard worse, not better.** #32 recorded that the ungated push to `main` was stopped because "branch protection rejected it; that rejection was the safety net working." **There is no branch protection on this repo:** `gh api repos/paulosalvatore/boraoke/branches/main/protection` → **404 `"Branch not protected"`**, `gh api .../rulesets` → **`[]`**. Both re-derived independently this fire. What actually refused the push was a plain **non-fast-forward**: `ticket/57` is 2 ahead but **4 behind** `main`. **The distinction is load-bearing, not pedantic** — branch protection refuses a push *because it targets `main`*, whereas non-fast-forward refuses only *because the branch happens to be stale*. The D-033 pattern branches off the current tip, so a **freshly-created** worktree is 1-ahead/**0-behind**: a clean fast-forward that **would have been ACCEPTED**, landing ungated code on `main` and auto-deploying live boraoke.com. All four existing branches are behind `main` (22/12/4/2) — so what #32 recorded as containment was **luck, and the next fresh worktree gets none of it**.
- **Finding 3 — the recorded blocker for enabling protection is itself stale.** The Notes line read "SKIPPED — GitHub Free + private repo (403)". **This repo is PUBLIC** (`gh api repos/paulosalvatore/boraoke` → `private: false`, `visibility: public`), and protection on public repos is free on the Free plan. The API answering **404-not-protected** rather than the predicted **403** independently corroborates that the endpoint is available. So the cost of the fix is zero and the reason for skipping it is obsolete. Corrected the Notes line in place (struck, not silently overwritten).
- **Finding 4 — a phantom task removed from the TL's list.** #32's 🔴 item instructed the TL to repair `ticket/57`'s upstream before committing from that worktree again. Re-audited **all four** worktrees: every one now has a correct upstream (`branch.<b>.merge = refs/heads/<b>`, remote `origin`), `ticket/57` included. `.git/config` was last written 2026-07-29 10:20 (after #32), consistent with the repair having been applied — **but this fire cannot attribute who applied it and does not claim to.** Marked resolved. **The framework-class fix is still genuinely open** and was NOT confused with the one-off repair: no `--no-track` at worktree creation, no refuse-to-push guard in `commit-and-push.sh`, and **both** inbox notes (`2026-07-25-worktree-upstream-defeats-commit-first-push.md`, `2026-07-28-worktree-upstream-points-at-main.md`) remain unprocessed. Existing worktrees are safe; the next one created still carries the landmine. Per CLAUDE.md §6 this stays reported, not fixed from a product tab.
- **Independent verification of the one claim that could have been an incident.** TICKET-59 asserts the public repo exposes no secret. Rather than trust it, delegated a **clean-context read-only verifier** to re-derive it over full history (`git log --all`). **VERDICT: CLEAN, agreeing with the claim** — `.env` / `.env.*` with `!.env.example` have been gitignored **since the bootstrap commit `4a55484`** (not bolted on after a leak); `.env.example` is the only env-family file ever added; `AIza`, `sk_live`, `sk_test`, `AKIA` and JWT-shaped tokens return **zero** matches across all history; every hit on `UPSTASH_REDIS_REST_TOKEN` / `NEXTAUTH_SECRET` / `ADVANCE_AUTH` is a `process.env` read, prose, a placeholder, or an explicit fixture (`faketoken`, `https://fake.upstash.io`, `gatetoken123`). So repo visibility is a **governance decision, not an incident** — and the two findings point in opposite directions, since public is exactly what makes TICKET-59 free.
- **Step 3 — NOTHING APPLIED, NO NEW PR.** TICKET-59 is **GATED**: it changes repo settings that govern the TL's own merge flow, and an unattended fire does not touch repo settings any more than it merges to a live-deploying `main`. Deliberately did **not** mint a fifth PR either — reaffirming #28/#29/#30: boraoke can never auto-merge unattended (every `main` merge auto-deploys the live client site), so the binding constraint is **human-merge throughput, not dev throughput**, and a fifth gate-green PR would grow an already-four-deep undeliverable queue for zero throughput gain. Recovering stranded work and correcting a false safety belief was the higher-value bounded increment, and it is genuine work rather than invented busywork.
- **Ball with the TL:** decide **TICKET-59** (enable branch protection — the payload is in the ticket; `enforce_admins` and forcing the PR flow are your preference calls) · answer the **repo public-vs-private** governance question (no secret exposure — verified) · batch-merge **#39, #40, #41, #42** (re-verified this fire as one cheap sitting, any order, zero rebase) · the two-step `cantai` clone cleanup (still holds the only local `.env`/`.env.local`, and is the root-cause fix for TICKET-58) · the growth-arc decisions · the ADVANCE_AUTH enforce flip. **Removed from your list:** the `ticket/57` upstream repair (phantom — already correct).
- **Selection safety:** boraoke checkout on `main` == `origin/main`; **created no worktree, no branch and no PR**; touched no other driver's worktree, branch or PR; no product source, no env var, no secret, no repo setting, no deploy; status-doc edits additive only (struck-through corrections, never silent overwrites).
- **Outcome: progressed.**

## 2026-08-06 — Heartbeat fire #35 (autonomous, unattended) — 🟡 IDLE (everything actionable is claimed by a live interactive TM session; no new PR)

- **Step 0 cold-resume + behind-origin guard (F193).** Canonical clone `~/Documents/GitHub/boraoke` (`git remote -v` → `git@github.com:paulosalvatore/boraoke.git`), HEAD on `main`, `git rev-list --count HEAD..origin/main` → **0** after fetch. Verified-at-0 → board authoritative.
- **Step 0.5 — board was already current, not stale.** `BOARD.md`'s banner (`_Last updated: 2026-08-06 ..._`) was itself written by an **interactive TM session with the TL present** earlier today (NOT a heartbeat fire): the pile drained (7 PRs squash-merged: #39–#44, #47), TICKET-59 (branch protection) closed WON'T-DO, a bot-prevention vendor decided (Cloudflare Turnstile), and a new merge-cadence rule recorded (TM auto-merges gate-green backend/test/docs-only PRs, TL gates patron-facing ones). `curl -sI https://boraoke.com/` → **HTTP/2 200** (live), consistent with the banner. No reconciliation needed this fire.
- **Parallel-driver check — a LIVE driver is active right now, not a historical one.** `gh pr list --state open` → **#45 (TICKET-61), #46 (TICKET-62), #48 (TICKET-65)**, matching the board exactly. `git worktree list` shows all three have open worktrees (`.worktrees/ticket-61`, `ticket-62`, `ticket-65`). `ps aux` found a **currently-running** process (`PORT=3165 npx playwright test`, started 20:08:50, still running at fire time 23:09 UTC — 2h07m elapsed) inside `.worktrees/ticket-65`, and the board banner records the interactive TM **sent PR #48 back minutes before this fire** after finding a real `host-controls.spec.ts` failure its own full-suite run caught — so that live process is very plausibly the same session actively re-verifying #48 right now. The TL also merged PR #43/#44/#47 within the hour immediately preceding this fire (`gh pr view --json mergedAt` → 22:07:03Z, 22:07:06Z, 23:06:25Z; fire started 23:09Z). Nine separate `claude --dangerously-skip-permissions` processes were running fleet-wide at fire time, one of which is almost certainly this interactive TM tab.
- **Step 1 selection — every open item is CLAIMED, no unclaimed safe increment exists.** #45 and #46 are gate-green and explicitly held per the board's new decision-1 policy (patron/kiosk-facing → TL gates them) — not this fire's to touch or merge even if it wanted to. #48 was just explicitly rejected by the TM itself for a real regression and is under active re-investigation (live process in its worktree) — picking it up would race the session that owns it. No other backlog item was visible as unclaimed/actionable within the bound of one heartbeat fire. Per the skill's parallel-driver guidance ("when in doubt, idle" / never touch a worktree you did not create), this fire did **not** create a worktree, branch, or PR, and did not merge anything.
- **Ball with the TL / the active interactive session:** review + decide **PR #45** and **PR #46**; continue the **PR #48** re-investigation already in flight. Nothing new to add from this fire.
- **Selection safety:** boraoke checkout on `main` == `origin/main` throughout; **created no worktree, no branch, no PR**; touched no other driver's worktree, branch or PR; no product source, no env var, no secret, no deploy; this log entry is the only edit, additive.
- **Outcome: idle.**
